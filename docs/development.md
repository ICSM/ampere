# Ampere v2 — development handoff notes

Audience: humans joining or reviewing the v2 redevelopment, and the person
dispatching agents. Agents themselves should start from `AGENTS.md`.

## The three documents

1. `DEVELOPMENT_PLAN.md` — decisions, architecture, phases, traps. The
   source of truth; changes to decisions go through its decision log.
2. `WORK_ITEMS.md` — agent-sized items for Phases 0–1, the working
   agreement, and the status table (updated at merge, not by agents).
3. `AGENTS.md` — ground rules and environment for any coding agent
   (Claude Code reads it via the `CLAUDE.md` symlink; Codex reads it
   natively).

## Dispatching work

- Each item is dispatched to one agent, on one branch (`w0.2-repo-hygiene`
  style), producing one PR. Respect the **Depends** lines in WORK_ITEMS.md;
  items with no unmet dependencies can run in parallel.
- Claude Code: point a session/agent at the item id — the `work-item`
  project skill carries the procedure. Codex: `codex exec` with a
  self-contained prompt; see `.claude/skills/delegate-codex/SKILL.md`.
- Agents do not push unless told to; review happens locally or on pushed
  branches at Peter's discretion.
- **Harness trap (hit and solved 2026-09-01)**: subagent worktrees may be
  created from the *session-start* commit rather than current HEAD. Every
  dispatch prompt must state the intended base commit and the recovery:
  verify the tree is clean and HEAD is a strict ancestor of the target
  (`git merge-base --is-ancestor HEAD <target>`), then branch directly
  from the target; STOP and report otherwise. Both W1.5/W1.6 agents
  recovered cleanly with this.
- **Harness trap (hit and solved 2026-09-03)**: `!`-prefixed commands
  typed by Peter run in the orchestrating session's *persistent shell
  cwd*. If that shell was last in an agent worktree, a `git merge` typed
  there silently no-ops ("Already up to date"). The orchestrator must
  return its shell to the main checkout before handing over any git
  command to run.

## Review checklist (per PR)

- Acceptance criteria evidenced in the report, not just claimed.
- Diff stays inside the item's scope; frozen legacy modules untouched
  (`ampere/data`, `ampere/models`, `ampere/infer`) unless the item says so.
- No run outputs, binaries, or checkpoints; British English in prose.
- From Phase 1 on: conformance suite green; contract changes carry a
  decision-log entry.
- On merge: update the WORK_ITEMS.md status table.

## Environment

- **pixi is the supported route** (W0.8, landed): from a clean clone with
  only `pixi` installed, `pixi install -e dev` sets up the daily-use
  environment (Python 3.13 + the `dev`, `zeus`, `extinction` extras).
  pyproject.toml's `[tool.pixi.*]` tables wrap `[project.dependencies]` /
  `[project.optional-dependencies]` — pixi never duplicates a dependency
  pyproject.toml already declares, it just adds Python (from conda-forge)
  and installs `ampere` itself as an editable PyPI dependency with the
  extras a given feature needs. `pixi.lock` is committed so environments
  are reproducible; `.pixi/` (the installed environments themselves) is
  not, and is gitignored.
  - Common tasks: `pixi run test` (fast: `tests/test_imports.py`),
    `pixi run test-all` (every new-namespace suite — core, results,
    conformance, backends, inference and, since W2.11, examples — in ONE
    pytest process; **this is the main gate**, and `test-phase1` remains
    the older core+results+conformance subset; the individual `test-core`,
    `test-results`, `test-backends`, `test-inference`, `test-examples` and
    `conformance` tasks exist too),
    `pixi run test-characterisation` (legacy still works),
    `pixi run lint`, `pixi run format-check`, `pixi run typecheck`,
    `pixi run docs` (repaired at W2.11 — it builds), `pixi run bench` (the
    tracked benchmarks; writes `benchmark.json`, which CI uploads as a run
    artefact) and `pixi run scaling` (the on-demand 10³–10⁵ GP scaling
    demonstration, deliberately in no gate). Since W0.10, plain
    `pixi run <task>` equals `-e dev`.
  - Other environments (`pixi run -e <env> <task>`): `test-py312` /
    `test-py313` / `test-py314` (the CI matrix, one Python each); `sbi`
    (since W3.2 the `dev` set plus the `torch` feature and the `sbi`
    extra — sbi 0.27, torch, pyro — and a real gate of its own:
    `pixi run -e sbi test-all`, plus `pixi run -e sbi typecheck`); `torch` /
    `jax` (the backend environments — real gates since W2.4/W2.5:
    `pixi run -e torch test-all`, `pixi run -e jax test-all`, and each
    backend package is typechecked with its real types only in its own
    environment). Since W2.11 both `torch` and `sbi` resolve torch from
    PyTorch's **CPU** index, so neither pulls ~2 GB of CUDA runtime onto a
    CPU-only runner; this changes nothing about what `pip install
    "ampere[torch]"` gives a user. **Run five-suite gates one at a time** —
    two concurrently exhaust a 13 GB machine. Torch's five-suite gate takes
    12–17 min here, sbi's 14–20 min, jax's 8–13 min, dev's 5–8 min.
  - **CI's `suites` matrix** (`.github/workflows/ci.yml`): one blocking
    job per new-namespace environment, each producing a check named
    "new-namespace suites (`<environment>`)" — `dev` (its own `suites` job:
    `test-all` + `bench`) and, in the `backend-suites` job's matrix,
    `torch`, `jax` and, since W3.7, `sbi` (each: `test-all`; `torch`/`jax`
    also run `bench`, deliberately not repeated for `sbi` since it would
    only re-exercise torch's own benchmarks under another name — see the
    job's comment). `sbi`'s install is cached the same way as `torch`'s
    (`setup-pixi`'s `cache: true`). The weekly/on-demand
    `sbi-characterisation` job is unrelated: it exercises the *legacy*
    `ampere.infer.sbi` flow, not the new-namespace `sbi` gate, and stays
    non-blocking.
  - **W4.10 — `typecheck` split out of the backend legs.** Since W4.10,
    `pixi run -e <env> typecheck` for `torch`/`jax`/`sbi` runs in its own
    matrix job, `backend-typecheck` (check name
    "typecheck (pyrefly, `<environment>`)"), independent of
    `backend-suites` (no `needs:` between them) — the two run concurrently
    rather than one serialising in front of the other, so the slow `sbi`
    leg's critical path in `backend-suites` is `test-all` alone. This was
    W3.7's finding: the `sbi` leg's local reference time (14–20 min) sits
    above the `torch` leg's (12–17 min), and splitting `typecheck` out was
    one of the two cheap levers considered there without widening that
    item's scope to touch test budgets. `dev`'s own `typecheck` job is
    unchanged (it already ran on its own).
  - **W4.10 — path gating.** A `changes` job runs first and computes, from
    a pull request's changed files, which of six buckets are touched —
    `run_dev`, `run_torch`, `run_jax`, `run_sbi`, `run_nested`
    (W5.14), `run_docs` — using
    `.github/scripts/path_filters.py` as the single executable source of
    the rule table (reproduced as a comment at the top of `ci.yml` and
    below). Every job in the workflow always runs; a bucket controls
    whether that job's *steps* do real work or print a one-line skip note,
    so an unaffected leg's check still reports **success**, never
    "skipped" or "expected" — the latter is what a top-level
    `on.push.paths`/`on.pull_request.paths` filter would produce (the
    workflow never triggers at all on an unaffected PR, so a required
    check named in branch protection never gets a status and the merge
    button hangs waiting for it). Push to master, `workflow_dispatch` and
    `schedule` runs are never gated — path gating only narrows a
    *pull_request* diff's turnaround time, never what lands on master or
    what a human explicitly asked to run.

    The path-filter table (buckets are exclusive; a path is classified by
    the first rule that matches — see the script's module docstring for
    the authoritative version):

    | Path pattern | Runs |
    | --- | --- |
    | `ampere/inference/_blackjax.py`, `tests/inference/test_blackjax.py` (W5.14; matched *before* the CORE row below) | dev, jax (blackjax installs into the existing jax environment, so it rides that leg) |
    | `ampere/inference/_nested.py`, `tests/inference/test_nested.py` (W5.14; matched *before* the row below) | dev, nested |
    | `pyproject.toml`, `pixi.lock`, `.github/**`, `ampere/core/**`, `ampere/results/**`, `ampere/inference/**`, and the shared test suites (`tests/{core,results,inference,conformance,m2,benchmarks,scaling,gpu,characterisation}/**` | everything: dev, torch, jax, sbi, nested, docs |
    | `ampere/backends/torch/**`, `tests/backends/*torch*` | torch, sbi (sbi's environment installs torch too) |
    | `ampere/backends/jax/**`, `tests/backends/*jax*` | jax |
    | `ampere/backends/reference/**`, remaining `tests/backends/**` | dev only |
    | `examples/sbi/**` | dev, sbi |
    | `examples/interferometry/**` (W4.4, not landed at W4.10 — a marked slot) | dev, torch, jax, sbi (conservative; W4.4 should narrow this to the backend(s) each example file actually exercises) |
    | `examples/**` (remainder), `tests/examples/**` | dev only |
    | `docs/**`, any `*.md` | dev, docs |
    | anything unrecognised | everything (same as the first row) |

    Locally: `python .github/scripts/path_filters.py --paths <files...>`
    (or `--stdin` with a newline-separated list) prints the bucket for each
    path and the resulting job list — the same thing the `changes` job
    prints in its own log. This is also how the item's three synthetic
    diffs were evidenced (W4.10's report).
  - **W4.10 — `actionlint`.** `pixi run -e dev actionlint` (a conda-forge
    binary, alongside `pandoc` in the `dev` feature) validates
    `.github/workflows/*.yml`; CI's `actionlint` job runs the same task,
    ungated (cheap, and useful on every PR — not only ones touching a
    workflow file). Every `uses:` in `ci.yml` is pinned to a full commit
    SHA with a `# vX.Y` comment (pinact style), and
    `.github/dependabot.yml` enables the `github-actions` ecosystem
    (weekly) so a version bump arrives as a reviewable PR updating both the
    SHA and its comment together.
  - **Required-check names for branch protection** (unchanged names keep
    their existing branch-protection entries; new ones need adding):
    `lint + format-check`, `actionlint`,
    `typecheck (pyrefly, new namespaces)`, `test (py312)`, `test (py313)`,
    `test (py314)`, `new-namespace suites (dev)`,
    `typecheck (pyrefly, torch)`, `typecheck (pyrefly, jax)`,
    `typecheck (pyrefly, sbi)`, `new-namespace suites (torch)`,
    `new-namespace suites (jax)`, `new-namespace suites (sbi)`,
    `typecheck (pyrefly, nested)`, `new-namespace suites (nested)`
    (both new at W5.14 — they need adding to branch protection),
    `docs build`, `minimal install (no extras)`. (`path filters` — the
    `changes` job itself — and the weekly/on-demand
    `characterisation suite (with sbi extra)` are not required checks: the
    former is plumbing every other job depends on, not a signal in its own
    right, and the latter is deliberately non-blocking, as before W4.10.)
- Plain-pip alternative: `pip install -e ".[dev]"`, Python ≥ 3.12 (raised 2026-09-22).
- Conda env `ampere` (Python 3.13) has an editable install pointing at the
  main checkout — worktree-based work that needs importing its own changes
  should use pixi or `pip install -e .` into a fresh env instead.
- `pytest tests/characterisation` (or `pixi run test-characterisation`) is
  the "legacy still works" gate; run it before merging anything that
  touches shared files.

## ⚡ Pick up here — PHASE 5 IN FLIGHT; **seventeen items merged (W5.0, W5.2, W5.20, W5.4, W5.3, W5.5, W5.13, W5.6, W5.7, W5.23, W5.9, W5.22, W5.11, W5.10, W5.8, W5.12, W5.14); the wave-4 merged gate re-run on 2026-09-22 green in all four environments and recorded; Peter's six rulings taken 2026-09-22 and five filler items added (W5.25–W5.29); master mirrored to `origin/v2` at `3cc97c7` (never to `origin/master`); W5.12 merged at `c26ff66`, W5.14 at `9347175` and **W5.25 at `51d18a6`** (2026-09-22, eighteen merged), rows pasted, no worktrees, no agents in flight; **the wave-5 merged gate (dev → sbi → torch → jax → nested) launched on master at ~10:20 after the `nested` install — to be recorded in the W5.12, W5.14 and W5.25 rows**; **the next session records the gate, then dispatches the next fillers: W5.26 (Sonnet M) ∥ W5.27 (Sonnet S)**

**What a clean session does, in order (written 2026-09-22 ~03:30 by Fable in the rulings session; everything below this paragraph is history).** (1) **The wave-4 gate is complete and recorded** (all four legs green on 2026-09-22; the rows carry the counts) — nothing to do here; the paragraph that follows is kept as the recipe for the next gate. **Recipe.** It covers W5.11 + W5.10 + W5.8 on master `264fbd4`, legs one at a time through `flock /tmp/ampere-gate.lock`. The summary log is now **durable**: `~/.cache/ampere-gates/wave4b-gate.log` (one `=== <env> DONE rc=<rc> <min> min: <pytest line>` per leg, full output beside it as `wave4b-gate.log.<env>`), and a leg is launched as `nohup setsid ~/.cache/ampere-gates/gate-leg.sh <env> wave4b-gate > /dev/null 2>&1 &` (the script is on the machine, not in git — recreate it from this paragraph if missing: cd to the checkout, append a START line, `flock /tmp/ampere-gate.lock pixi run -e <env> test-all` into the per-env file, append the DONE line). The dev leg is recorded (2955 passed / 446 skipped, 28 min 17 s; baseline on `dd79aa0` 2827/423 in 20 min — the rise is the three items' rows and W5.8's `tests/m2`). The sbi leg is recorded too (4046 / 101, 56 min 19 s). All four legs are recorded (dev 28 min, sbi 56, torch 49, jax 44 — 2 h 57 m end to end; baselines on `dd79aa0`: 20/46/41/47 min). The next merged-wave gate is launched the same way, one leg after another, base = master after the merges. Baselines: sbi 3900/96 (46 min), torch 3766/230 (41), jax 3586/239 (47). Paste each count into the W5.11, W5.10 and W5.8 status rows (each now says "sbi, torch and jax to be recorded"). A failure comes back to a cheap fix-up agent with the log excerpt. (2) **Check the CI run on `v2` after the `frozen` fix is pushed** (`gh run list --branch v2 --limit 3`, `gh run view <id>`): the first run (`35679246921`, on `71c1638`) failed every job at pixi setup on the `--locked` check described in the session record; the fix is committed on master and needs Peter's push (`git push origin master:v2`). If the next run is red for a different reason it is W5.26's input if about layout or cost, and a fix-up agent's if a real defect. (3) **W5.25 merged by Peter at `51d18a6`; its row pasted and the W5.8 note appended; the last worktree removed; the wave-5 gate launched** (the `nested` environment installed in the main checkout first, `pixi install -e nested --frozen`, then five legs through `~/.cache/ampere-gates/gate-leg.sh <env> wave5-gate`, summary log `~/.cache/ampere-gates/wave5-gate.log`; baselines from the wave-4 re-run: dev 2955/446 in 28 min, sbi 4046/101 in 56, torch 3896/251 in 49, jax 3716/260 in 44; nested has no baseline — expect dev's count plus the nested battery; the W5.12, W5.14 and W5.25 rows each say "the wave-5 merged gate to be recorded"; a failure comes back to a cheap fix-up agent with the log excerpt). **(Record):** W5.14 merged at `9347175`; W5.25 was reviewed and ready. W5.25's second round landed `_hierarchical_gamma` (`32b1d0e`); Fable read the four builders directly (both backends follow `halfnorm`'s pattern exactly; the hierarchical gamma mirrors the flat one with the shape argument folded in as a tensor); re-runs green in dev, jax and torch including the torch default-tail row the extension exists for; hygiene clean; `git merge-tree` clean against master **and** against `w5.14-tier1-engines`. **Peter runs, from the main checkout, after W5.14**: `git merge --no-ff w5.25-halfcauchy-lowering -F /tmp/claude-1000/-home-pscicluna-GitHub-ampere/0ce2919a-b6b7-4348-b0c5-a3692f1e0f3b/scratchpad/w5.25-merge-message.txt`. Then the orchestrator pastes the row below, appends the note below to the **W5.8** decision-log row (no new row: a §3.2 table addition is contract-adjacent), removes the worktree (`git worktree remove --force --force .claude/worktrees/agent-a237dcb9a8d992b5b`), and the wave-5 merged gate covers W5.12 + W5.14 + W5.25.

*W5.25 status row (WORK_ITEMS.md, after W5.14's)*:

| W5.25 | merged 2026-09-22 at `MERGE_HASH` (Sonnet-authored, Fable-reviewed as the second pass — the four builders read directly; **no fixes needed**; **extended by the orchestrator at the first report** to close the gap the item found (below); merged by Peter; the orchestrator's re-runs on the final head through the lock: dev `test_halfcauchy.py` + `test_parameter.py` 174 passed in 1.9 s, jax `test_halfcauchy.py` + `test_jax.py` + `TestTheHorseshoeUnderNUTS` 224 passed in 67 s, torch `test_halfcauchy.py` + `test_torch_lowering.py` + `TestTheHorseshoeUnderNUTS` 218 passed / 2 skipped in 12 min 10 s (both tails pass on torch; the two skips are pre-existing); hygiene clean; the agent: ≈ 305 k tokens / 243 tool uses / 41 min across the two rounds; the wave-5 merged gate to be recorded with W5.12 and W5.14). Landed: `halfcauchy` in both backends' tables — torch `_build_halfcauchy`/`_halfcauchy` (`HalfCauchy(scale)` at `loc == 0`, the §3.3 affine shift otherwise) and `_hierarchical_halfcauchy` in `_HIERARCHICAL_BUILDERS`; jax `_halfcauchy` (`HalfCauchy`, or `TruncatedCauchy(loc, scale, low=loc)` shifted, on `_halfnorm`'s support reasoning) and `NATIVE_ICDF`; `lowering.md` §3.2 row and §3.2.1 rewritten ("the horseshoe's chain now lowers, in full"); the `regularised_horseshoe` docstring; `tests/conformance/test_halfcauchy.py` (prior transform against scipy's `ppf`, log-density against `logpdf`, no mass below `loc`, cube centre is the median; `loc ∈ {0, 3}`); `TestTheHorseshoeUnderNUTS` in `tests/inference/test_nuts.py` on W5.8's actual `horseshoe_kernel` declaration, both tails, torch and jax. **The finding and the extension**: torch keeps a separate per-family registry for hierarchical priors (`_HIERARCHICAL_BUILDERS`: `norm`, `uniform`, `halfnorm`, `expon`, now `halfcauchy`) while jax lowers hierarchical priors generically from the flat table, so `tail="regularised"`'s `gamma` local scale still failed under NUTS on torch — pre-existing, isolated from `halfcauchy`, and contradicting §3.2.1's earlier claim; the orchestrator extended the item on the spot: `_hierarchical_gamma` (mirroring `_build_gamma`, the fixed `a=½` folded in by `lower_hierarchical` as any constant), a torch backend row against the flat table and scipy to 1e-9, the skip removed. **For Peter**: none. Carried: the torch horseshoe NUTS rows cost ≈ 5 min per tail (the funnel; W5.26's territory if the budget matters); `_default_bijection_for_hierarchical` refuses any family with a shape argument unconditionally, which is why the horseshoe still passes `bijection=Log()` explicitly — documented, a candidate for W5.28. |

*Note to append to the W5.8 decision-log row*: W5.25 (merged 2026-09-22 at `MERGE_HASH`): `halfcauchy` now lowers on both backends (one flat-table row each, plus torch's hierarchical-registry row), and torch's hierarchical registry also carries `gamma` (found and closed in the same item), so `regularised_horseshoe`'s full three-level declaration reaches NUTS on both backends under both tails — confirmed by `TestTheHorseshoeUnderNUTS` (jax 2 passed; torch 2 passed).

**(Record):** W5.14 reviewed and ready; W5.25 reported and extended. W5.25's first report (Sonnet, ≈ 251 k tokens / 188 tool uses / 32 min): `halfcauchy` lowers on torch (`_build_halfcauchy` + `_hierarchical_halfcauchy` in the per-family `_HIERARCHICAL_BUILDERS`) and jax (`_halfcauchy`, `TruncatedCauchy` when shifted, `NATIVE_ICDF`), the §3.2 row, `tests/conformance/test_halfcauchy.py` (16/24/24 across dev/torch/jax), `TestTheHorseshoeUnderNUTS` in `tests/inference/test_nuts.py` on W5.8's actual declaration (jax 2 passed in 16 s; torch `cauchy` passed in 327 s, `regularised` **skipped by name**) — and the finding: **torch keeps a separate per-family registry for hierarchical priors and it has no `gamma`**, so the default tail still fails under NUTS on torch while jax lowers it generically from the flat table. A pre-existing gap, isolated from `halfcauchy`. The orchestrator extended the item on the spot (the item's purpose is that the recommended prior reaches NUTS on both backends): a `_hierarchical_gamma` builder mirroring `_build_gamma`, the torch row un-skipped, one torch backend row, §3.2.1 corrected, the W5.8 note amended. Review after the second report as usual.  Re-runs on the final head `cdb87a0` through the lock, all green: dev 337 / 343 in 37 s, nested 57 / 6 in 75 s, torch 62 / 49 in 2 min 14 s, jax 95 / 51 in 6 min 54 s; hygiene clean; `git merge-tree` against master clean; the worktree's `pixi.lock` unchanged by the nested install. **Peter runs, from the main checkout**: `git merge --no-ff w5.14-tier1-engines -F /tmp/claude-1000/-home-pscicluna-GitHub-ampere/0ce2919a-b6b7-4348-b0c5-a3692f1e0f3b/scratchpad/w5.14-merge-message.txt` (the message body is in the W5.14 row's first sentence and the file; if the scratchpad is gone, write it from the row). Then the orchestrator pastes the three rows below with the hash, removes the worktree (`git worktree remove --force --force .claude/worktrees/agent-abce379f3c327c31b`), installs `nested` in the main checkout (`nohup setsid bash -c "flock /tmp/ampere-gate.lock pixi install -e nested --frozen" &`) and launches the wave-5 merged gate: five legs, dev → sbi → torch → jax → nested, `~/.cache/ampere-gates/gate-leg.sh <env> wave5-gate`.

*Status row (WORK_ITEMS.md, after W5.12's)*:

| W5.14 | merged 2026-09-22 at `MERGE_HASH` (Opus-authored across two agents — the first landed the nested pair and handed off at its natural boundary under the 2026-09-22 budget rules (≈ 339 k tokens / 180 tool uses / 34 min), the successor landed the VI guides and the blackjax route (≈ 367 k / 238 / 70 min); Fable-reviewed as the second pass — the shared nested driver, the Laplace and flow handling and the blackjax refusals read directly, the lock's environment diff verified (jax +blackjax/optax/absl-py, `nested` new, all else byte-identical); **two fixes at review**: `70f0128` (the MCLMC dimension floor declared twice) and `cdb87a0` (`results.md` §9's `ampere_approximation` vocabulary and the evidence triple's absence as a state); merged by Peter; the orchestrator's re-runs on the final head through the lock: dev `tests/inference` + `test_parameter.py` 337 passed / 343 skipped in 37 s, nested `test_nested.py` + doctests 57 / 6 in 75 s, torch `test_vi.py` + `test_nested.py` + doctests 62 / 49 in 2 min 14 s, jax `test_vi.py` + `test_blackjax.py` + `test_nested.py` + doctests 95 / 51 in 6 min 54 s; hygiene (lint, format, typecheck dev, actionlint) clean; the agents' runs: nested `test_nested.py` 50 / 3 in 60 s, dev inference battery 117 / 45, torch `test_vi.py` + doctests 51 / 7 in 126 s, jax `test_blackjax.py` + doctests 41 / 4 in 99 s, dev `tests/inference` 179 / 343, typecheck jax/torch/nested 0 errors, docs build clean; the wave-5 merged gate to be recorded with W5.12, **now five legs**). Landed: `ampere/inference/_nested.py` — `NautilusEngine` and `UltranestEngine` over `_NestedEngine` (dynesty's `resample_equal` on the engine's `resample` stream as the one weighted-draw implementation for all three nested samplers; `ampere_evidence_method = "nested_sampling"` for all three, closing W5.0's carried question as the method family; nautilus's error as `1/sqrt(n_eff)` with `ampere_nautilus_log_z_err_source`; a one-dimensional problem refused by name; ultranest under `engine.global_seed`, shared with zeus; `engine.default_live_points` shared with dynesty); `_vi.py` — `GUIDE_FAMILIES` gains `laplace` (`AutoLaplaceApproximation`; a `Delta` until asked for its Gaussian: pyro's `laplace_approximation()`, numpyro's `get_posterior(params)`) and `flow` (`AutoIAFNormal`; the base moved to the seeded start through `get_base_dist`, pyro's network in float64; `ampere_vi_guide_parameters`; below two free parameters refused by name); `_blackjax.py` — `BlackjaxEngine(problem, method="mclmc"|"pathfinder")`, jax-only by nature (MCLMC tuned by `mclmc_find_L_and_step_size`, one `lax.scan`; Pathfinder as approximation writing `ampere_approximation="pathfinder"` and `proposal_log_density`, and as `initial="pathfinder"`; MCLMC writes `"none"` with `ampere_blackjax_adjusted`/`desired_energy_var`; neither writes the evidence triple, asserted absent); extras `nautilus`/`ultranest`/`blackjax` with 1:1 pixi features, one new environment `nested` (dev's set plus the two samplers) with a path-gated CI leg on both backend matrices, blackjax folded into `jax`, `NESTED` and `BLACKJAX` path buckets before `CORE`, `engines_full` marker; the batteries `test_nested.py` (dynesty the control; analytic log Z −13.0684: dynesty −0.15 σ, nautilus −0.26 σ, ultranest +1.46 σ; tolerance `max(4 σ, 0.4 nat)`), `test_vi.py` (Laplace exact on the conjugate Gaussian; the flow's density against the library's ELBO; every guide SBC-ranked), `test_blackjax.py`; `inference.md` §10 (the item said §5) "Three nested samplers on one surface" and "Four guide families and a second gradient library", `results.md` §9 (review), the memo's landed notes, `advanced.rst`, `ampere.inference.rst`. **Rulings for Peter**, none blocking: (1) a separate `nested` environment and CI leg rather than folding the samplers into `dev` — recommend keep; (2) nautilus's `1/sqrt(n_eff)` as ampere's own labelled evidence error — recommend accept; (3) MCLMC's `ampere_approximation="none"` rather than a named unadjusted family — recommend keep, the key answers whether chain diagnostics apply; (4) the flow's base-distribution subclass and `_DESIRED_ENERGY_VAR` duplicating a library default — recommend accept with their comments. Carried: `path_filters.py`'s `DEV_JOBS` list still names py311–313 (W5.18, one line); ultranest's `logzerr` is more conservative than its console line; `proposal_log_density` has four producers and no cross-engine conformance row; the jax `test_vi.py` leg's budget cut from 22 min to ~2 (a flow draw traces three networks; each SBC replica is a fresh jax compilation) — W5.26's territory if it grows again. |

*Decision-log rows (DEVELOPMENT_PLAN.md §2, after W5.12's row, in this order)*:

| Tier-1 nested sampling behind extras (W5.14, first agent) | **`nautilus` and `ultranest` ship as `NautilusEngine`/`UltranestEngine` over a shared `ampere/inference/_nested.py`, one extra per sampler** (`inference_extensions_memo.md` §6 tier 1, §10's 2026-09-10 ruling; merged 2026-09-22 at `MERGE_HASH`), joining `DynestyEngine` on `prior_transform`. `inference.md` §10's portability claim is checked by three libraries of the same kind for the first time and **nothing in the contract changed to admit them**: both consume `prior_transform` and `log_likelihood` and nothing else. Four consequences recorded. (a) **`results.md` §9's weighted-draw rule is one implementation for all three** — `dynesty.utils.resample_equal` on the engine's own `resample` stream — because three nested samplers must not be able to produce three slightly different posteriors from the same dead points; each library's own equal-weight output is unused, since each draws from its library's randomness rather than the problem's seed. (b) **W5.0's carried question is answered: `ampere_evidence_method` names the method *family*, not the engine** — `"nested_sampling"` for all three — because `ampere_engine` already names the engine and what a reader needs from the second attribute is whether two archived evidences were estimated the same way. (c) **nautilus reports no evidence uncertainty**, so ampere records the importance-sampling estimate `1/sqrt(n_eff)` over the library's own Kish effective size and says so in `ampere_nautilus_log_z_err_source`; it also refuses a one-dimensional problem by name, which is why the closed-form linear-Gaussian evidence case is run at **two** free parameters rather than W3.6's one. (d) **Design horizon (g)'s cost record is `engine_evaluations`**, which `Engine.finish` already writes for every run, beside each sampler's own likelihood-call count — no new engine-neutral attribute, as §10's ruling that (e) to (g) need no contract change today requires. Two helpers became shared rather than duplicated: `engine.default_live_points` (out of `_dynesty.py`) and `engine.global_seed` (out of `_zeus.py`, because ultranest draws from numpy's process-global generator exactly as zeus does — `inference.md` §12's thread-safety limitation now has two owners). Packaging: `nautilus`/`ultranest`/`blackjax` extras with 1:1 pixi features; **one** new pixi environment, `nested` (dev's feature set, numpy-only), with a path-gated CI leg on both backend matrices, while **blackjax folds into the existing `jax` environment** — a jax-only driver has no other environment it could run in. The **engine battery** (`tests/inference/test_nested.py`) is the memo's §6 prescription made executable, with dynesty in it as the control; measured against analytic log Z = −13.0684: dynesty −0.15 σ, nautilus −0.26 σ, ultranest +1.46 σ. |

| The rest of tier 1: variational guide families and a second gradient library (W5.14, successor agent) | **`VIEngine` gains the guide families `laplace` and `flow` on both routes, and `BlackjaxEngine(problem, method=...)` lands jax-only with MCLMC and Pathfinder** (merged 2026-09-22 at `MERGE_HASH`; Opus-authored across two sessions, the second continuing the first's branch under the 2026-09-22 hand-off rule). Three things the inference-extensions memo's §2 did not anticipate, each found by running the code rather than reading the libraries. (a) §2.2's "Laplace is a new entry in `GUIDE_FAMILIES` and nothing else" is false: in **both** libraries the autoguide is a `Delta` during optimisation, so SVI fits the MAP location and the Gaussian exists only afterwards — pyro's `laplace_approximation()` returns an `AutoMultivariateNormal`, numpyro's `get_posterior(params)` builds one from the Hessian on demand — and a driver that drew from what SVI fitted would return the mode `draws` times. (b) A flow guide cannot be started with `init_loc_fn` (both libraries ignore it) and in ampere's unconstrained coordinates it must be started somehow, a parameter with an unbounded prior being its own coordinate: measured at `norm ≈ 1.1` after 4000 steps against a posterior at `2.01 ± 0.10`, against mean-field's 600 steps. Both routes move the flow's **base** to the same seeded prior draw through `get_base_dist`, the hook both libraries define; pyro's autoregressive network is additionally built in float64 explicitly, `lowering.md` §10.1 forbidding `set_default_dtype`. (c) `ampere_approximation`'s meaning is settled by the blackjax pair: Pathfinder writes `"pathfinder"` with a per-draw `proposal_log_density` (blackjax returns the density beside its draws, so §5.2b costs only the coordinate change), while **MCLMC writes `"none"` despite being an unadjusted chain with a discretisation bias** — the key answers "do chain diagnostics apply?", and for a Markov chain they do; the bias is recorded under the engine's own names and measured by the battery's SBC. Neither blackjax method writes the engine-neutral evidence triple, and the battery asserts its *absence*: MCLMC samples an unnormalised density, and Pathfinder's ELBO is one L-BFGS path's lower bound of unknown tightness, which under a nested sampler's attribute name would invite a meaningless comparison. `ampere_approximation`'s vocabulary widens by `"laplace"` and `"normalising_flow"` with no reader change (every consumer tests `!= "none"`), **`results.md` §9's example list amended at review** to say so and to name the evidence triple's absence as a state; the new `ampere_vi_guide_parameters` records which fitted parameters a run left on the engine — `"none"` for a flow, whose fit is a network's weights and whose `guide_loc`/`guide_scale`/`guide_scale_tril` all stay `None`. Two floors refused by name in ampere's words before a library raises: a flow below two free parameters, and MCLMC below two (the isokinetic dynamics decorrelate a *direction*). CI gains a `BLACKJAX` path bucket gated to the **jax** leg rather than a leg of its own, blackjax installing into the existing jax environment. **Carried**: whether MCLMC's `"none"` should instead be a named unadjusted family. |

**(Record, superseded):** W5.14 was complete and under review; W5.25 in flight. The successor (≈ 367 k tokens / 238 tool uses / 70 min, no second hand-off) landed the `laplace` and `flow` guides on both routes (`_vi.py`: the Laplace autoguide is a `Delta` until asked for its Gaussian — pyro's `laplace_approximation()`, numpyro's `get_posterior(params)`; the flow's base moved to the seeded start through `get_base_dist`, pyro's network built in float64; `ampere_vi_guide_parameters`; a flow below two free parameters refused by name) and `_blackjax.py` (`BlackjaxEngine(problem, method="mclmc"|"pathfinder")`, jax-only by nature and refusing other backends in the constructor; Pathfinder writes `ampere_approximation="pathfinder"` and `proposal_log_density`; MCLMC writes `"none"` with its bias recorded under the engine's names; neither claims an evidence; a BLACKJAX path bucket gated to jax), batteries in `test_vi.py` and the new `test_blackjax.py`, `inference.md` §10, the memo, the docs pages; a budget finding recorded (the jax `test_vi.py` leg cut from 22 min to ~2 by drawing 500 from the flow and 4 reduced SBC refits). **Fable's review**: the Laplace and flow handling and the blackjax refusals read directly; **two fixes on the branch** — `70f0128` (the MCLMC dimension floor declared twice) and `cdb87a0` (`results.md` §9's `ampere_approximation` vocabulary widened by `laplace`/`normalising_flow`/`pathfinder`, and the evidence triple's absence named as a state — the successor's open question 2); hygiene clean on the branch. Re-runs on the final head through the lock: dev `tests/inference` + `test_parameter.py` 337 passed / 343 skipped in 37 s; nested, torch, jax legs running (log `~/.cache/ampere-gates/w5.14-review.log`). Merge message drafted at `<scratchpad>/w5.14-merge-message.txt`; the status row and the **two** decision-log rows (both agents' texts, in their reports — the orchestrator holds them) are drafted at merge time. **Rulings the successor asks for** (none blocking): MCLMC's `ampere_approximation="none"` versus a named unadjusted family (recommend keep `none`: the key answers whether chain diagnostics apply); the flow's base-distribution subclass reaching into a library hook (recommend accept, it is the documented hook); `_DESIRED_ENERGY_VAR` duplicating a blackjax default (recommend accept with its comment). **W5.25 dispatched** at 08:55 (Sonnet) from `fc32700` in a harness worktree, branch `w5.25-halfcauchy-lowering`: the two lowering rows, the §3.2 row, a conformance row, NUTS rows for the shrinkage declaration — disjoint from W5.14's files. **(Record):** W5.14 was in its successor's hands. The first agent (≈ 339 k tokens / 180 tool uses / 34 min) landed `ampere/inference/_nested.py` (`NautilusEngine`, `UltranestEngine` over `_NestedEngine`; dynesty's `resample_equal` as the one weighted-draw implementation for all three nested samplers; nautilus's evidence error as `1/sqrt(n_eff)` with `ampere_nautilus_log_z_err_source` saying so; a one-dimensional problem refused by name; `engine.default_live_points` and `engine.global_seed` shared), the `nautilus`/`ultranest`/`blackjax` extras with 1:1 features, one new pixi environment `nested` (dev's set plus the two samplers) with a path-gated CI leg on both backend matrices, blackjax folded into the `jax` environment, `tests/inference/test_nested.py` (the battery with dynesty as the control; analytic log Z −13.0684: dynesty −0.15 σ, nautilus −0.26 σ, ultranest +1.46 σ; `engines_full` marker), `inference.md` §10 amended (the item said §5), the memo's landed note and the two docs pages; then handed off at the natural boundary with the remaining work in `5aea5b0`'s message. Fable read the landed half (the driver's key lines, the lock's environment diff verified: jax +blackjax/optax/absl-py, `nested` new, all else byte-identical; the NESTED path bucket) — nothing to fix so far; the full re-runs happen once on the successor's final head. **The successor's prompt** carries the hand-off verbatim: VI guides `laplace`/`flow` (per-guide fitted-parameter extraction with `None` allowed, `proposal_log_density` checked against an independent density — W5.0's defect site), then `_blackjax.py` (MCLMC; Pathfinder as initialiser and as approximation writing `ampere_approximation`), a BLACKJAX path bucket, batteries for each, the decision-log row for its two thirds. **Rulings the first agent asks for** (recorded for the merge): (1) one new `nested` environment and CI leg versus folding the samplers into `dev` — recommend keep the leg (the Accept line asks for path-gated legs and `dev` is every wave's gate); (2) nautilus's `1/sqrt(n_eff)` evidence error, ampere's number not the library's, labelled as such — recommend accept; (3) the `blackjax` extra locked ahead of its driver — moot once the successor lands it. Out of scope, noted: `path_filters.py`'s `DEV_JOBS` list still names py311–313 (W5.18's file; one line); ultranest's `logzerr` is more conservative than its console line (recorded honestly); W5.0's carried `ampere_evidence_method` question is answered in code (the method family) and closes at merge. **CI on the pushed floor change (`35688582595` on `3cc97c7`) is as predicted**: py312, py313 and the new py314 legs green, 15 jobs green in all, and only the two W5.26 (7) rows red — with the same values to every digit as the first run, so the runner is deterministic and a fix is verifiable by one push. **The wave-5 merged gate gains a fifth leg**: `pixi run -e nested test-all` (install `nested` in the main checkout after the merge: `pixi install -e nested --frozen`, detached). **(Record of W5.12's merge):** W5.12 merged by Peter at `c26ff66`; both rows pasted; the worktree removed. (Record of the review, superseded): Fable's second pass (Codex reviews remain unavailable): read `Population`, `_apply_populations`, `_element` and the decomposition row directly; the plate layout builds one array parameter per member in the population's own component with a per-element `PlateBinding` to each `over` component (references resolve without renaming), the flat layout renames onto the qualified hyperpriors and caps at 128; the refusals are by name and cover the cases the report lists; Peter's W5.22 note is answered, not actioned, for a sound reason (the joint fit never consults a stored interim prior; `results/population.py` untouched). The orchestrator's re-runs on the branch through the lock (`tests/conformance/test_population.py tests/inference/test_population_nuts.py tests/core/test_parameter.py`): dev 177 passed / 10 skipped in 2.6 s, torch 194 / 4 in 1 min 45 s, jax 194 / 4 in 17 s; lint, format-check, typecheck (dev) and the import sweep clean on the branch; `git merge-tree` against master clean. **Peter runs, from the main checkout**: `git merge --no-ff w5.12-population -F /tmp/claude-1000/-home-pscicluna-GitHub-ampere/0ce2919a-b6b7-4348-b0c5-a3692f1e0f3b/scratchpad/w5.12-merge-message.txt` (the message is also quoted below). Then the orchestrator pastes the two rows below with the merge hash, removes the worktree (`git worktree remove --force .claude/worktrees/agent-a05e68a5933b4e628`), and W5.12 joins the wave-5 merged gate with W5.14. **The agent's usage**: ≈ 340 k tokens / 391 tool uses / 56 min — inside the new budget, no hand-off.

*Merge message*: the file above; its body: "W5.12: Population, the composition-time hierarchical declaration (gap H-1 of hierarchical_population.md) and the joint population fit on the native path. ParameterSet.merge(..., populations=[...]) adds the hyperpriors as one further component and gives every component named in `over` its own draw under the member's bare local name, so per-object models compose into a population fit unmodified. Two layouts declaring one density: \"plate\" (the default; H-2's array-valued parameter with per-element Binding.index routing, a real numpyro/pyro plate on the native path) and \"flat\" (the tie-based pattern, refused above MAX_FLAT_MEMBERS = 128). DatasetCollection.plate(...) lifts inference.md limitation 17.6. One bug fixed in scope: ParameterMapping.distribute coerced element-bound values through numpy.asarray, which raised on a torch tensor requiring grad; it now indexes in the value's own array type. Neither backend needed a change. Opus-authored, Fable-reviewed; no fixes needed." plus the two trailers.

*Status row (WORK_ITEMS.md, after W5.8's)*:

| W5.12 | merged 2026-09-22 at `MERGE_HASH` (Opus-authored, Fable-reviewed as the second pass — `Population`, `_apply_populations`, `_element` and the decomposition row read directly; **no fixes needed**; merged by Peter; the orchestrator's re-runs on the branch through the lock: dev 177 passed / 10 skipped in 2.6 s, torch 194 / 4 in 1 min 45 s, jax 194 / 4 in 17 s (`test_population.py`, `test_population_nuts.py`, `test_parameter.py`); the agent's runs: dev `tests/conformance tests/results` 1076 / 74 in 11 min 23 s, `tests/core` 1386 / 30, torch and jax `tests/conformance` 1007 / 69 each, torch `test_population_nuts.py` 6 in 74 s, jax 6 in 13 s, hygiene clean; the wave-5 merged gate to be recorded with W5.14; the agent: ≈ 340 k tokens / 391 tool uses / 56 min, no hand-off — the first item under the 2026-09-22 budget rules). Landed: `ampere.core.Population(name, members, hyperpriors, over=, size=, layout=, label=)` and `MAX_FLAT_MEMBERS = 128`; `ParameterSet.merge(..., populations=[...])` (keyword-only, additive); the `plate` layout (one array parameter per member of shape `(size,) + member.shape` in the population's component, `PlateBinding` per element to each `over` component; lowers to a real plate on both backends with no backend change) and the `flat` layout (the tie-based pattern, references renamed onto the qualified hyperpriors, refused above 128); refusals by name at merge (taken label, unknown or self-referencing component, shape or unit disagreement, a fixed site, a tied or `shared_as` site, a component merged as a `ParameterMapping`); `DatasetCollection(..., populations=)`, `.populations`, `DatasetCollection.plate(name, datasets, members=, hyperpriors=, over=None, layout=, label=)` defaulting `over` to the datasets' distinct model labels, `FittingProblem(..., populations=)`; the `distribute` fix (`_element`); `tests/conformance/test_population.py` (once per fixture: the decomposition on both layouts, the realised population against the numpy path, the plate lowering against the flattened form) and `tests/inference/test_population_nuts.py` (50 members, 52 free dimensions, `mu` and `sigma` inside the central 95 %, member draws correlating > 0.9 with their truths); `parameters.md` §8/§9, `inference.md` §9/§10a and limitation 17.6 lifted, `hierarchical_population.md` H-1 dispositioned with its three deviations, §11 Q2 closed and Q5's joint-fit half landed, `docs/source/advanced.rst`. **Peter's W5.22 note**: `fit_population`'s refusal of a stored `HierarchicalPrior` is **not lifted** — the joint fit (horizon (a)) never consults a stored interim prior, and the marginal is only defined against an archived population fit, whose members' interim priors are not independent; real work for W5.13's successor, not this item. **For Peter**: (1) the layout is an explicit `layout=` with one default everywhere rather than backend-dependent behaviour (the agent's reading of "the numpy path fits small populations by the tie-based pattern"; recommend confirm — a backend-dependent parameter space would defeat the conformance comparison); (2) `MAX_FLAT_MEMBERS = 128` is a judgement call (recommend accept); (3) `over` cannot name a `Dataset` component — a population addresses members by bare local name and a dataset's merged names are qualified; relaxing it is a §4 change to `PlateBinding` or routing in two places (§11 Q1's rejected alternative); recommend leave refused and documented until per-dataset nuisance populations are wanted. Carried: the centred parameterisation is the only one expressible (a non-centred `θ_i = μ + σ z_i` needs the `Derived` node already recorded as Phase 6); `tests/inference` in dev is slow because of the SBI rows (W5.26's territory). |

*Decision-log row (DEVELOPMENT_PLAN.md §2, after the Python-floor row)*: the agent's text, verbatim from its report, with `MERGE_HASH`:

| `Population`: hierarchy declared at composition time, and the joint population fit (W5.12) | **Decided 2026-09-22 (merged at `MERGE_HASH`; Opus-authored, Fable-reviewed), discharging Peter's ruling of 2026-09-03 on `hierarchical_population.md` §11 Q2 (`Population` lands with Phase 5) and the joint-fit half of Q5.** `ampere.core.Population` is the hierarchical counterpart of `Tie`: `ParameterSet.merge(..., populations=[...])` adds the hyperpriors as one further top-level component and gives every component named in `over` its own draw under the member's bare local name, so N independently written per-object models compose into a population fit unmodified — which is the cost gap H-1 named. It ships **two layouts declaring one density**: `layout="plate"` (the default) produces H-2's array-valued parameter and per-element `Binding.index` routing, lowers to a real `numpyro`/`pyro` plate on the native path, and is unbounded; `layout="flat"` produces the tie-based pattern `parameters.md` §9 documents and is refused above `MAX_FLAT_MEMBERS` (128), which turns §7's measured O(number of `Parameter` objects) prior cost into a guardrail. The merge's signature change is additive and keyword-only, so no existing call and no conformance row moved. `DatasetCollection.plate(...)` lifts `inference.md` limitation 17.6; a population is refused over a component merged as a `ParameterMapping`, because a composite's merged names are qualified and a bare-named element would never reach a leaf — a plate of datasets declares its population over the models those datasets name. `ParameterMapping.distribute` now takes an element binding's index **in the value's own array type**: both backends' realisations route native values through the merge's table, and the previous `numpy.asarray` coercion raised on a torch tensor requiring grad (recorded in `inference.md` §10a). Amended: `parameters.md` §8/§9, `inference.md` §9/§10a/17.6, `hierarchical_population.md` §9 H-1 and §11 Q2/Q5. Conformance: `tests/conformance/test_population.py`; recovery: `tests/inference/test_population_nuts.py`. |

(3, earlier) **Wave 5 was dispatched** (W5.12 at 06:10 on 2026-09-22 as an Opus agent in the harness worktree `.claude/worktrees/agent-a05e68a5933b4e628`, branch `w5.12-population` from `0cdfa2c`; W5.14 an hour later from the same base — if this paragraph still says "to follow", check `git worktree list` and `git branch` for `w5.14-*` before dispatching it). Both prompts carry the item text, the read-ranges and the ownership split (W5.12: core, the backends' realisation, conformance/inference tests, the population sketch and contract pages; W5.14: `ampere/inference/`, `pyproject.toml`/`pixi.lock`/`.github/`, inference tests, the memo) and the Budget rules. If an agent dies (session limit), its committed work is on its branch: resume it with `SendMessage` or dispatch a successor with the branch's last commit message as the hand-off. **The original note**: Dispatch wave 5: W5.12 ∥ W5.14 (both Opus; W5.12's text carries the note on lifting `fit_population`'s hierarchical-prior refusal), prompts from the standing paragraph below plus each item's read-ranges, ownership and Accept list, base = master after the gate, fresh worktrees, staggered by an hour, dispatched when no gate leg is running. **The wave-5 prompts are the first under the 2026-09-22 budget rules** (`docs/orchestration.md` "Re-evaluation", the `work-item` skill's Budget section): after the first agent's first hour, read its transcript's cache tier (`grep -c 'ephemeral_1h_input_tokens":[1-9]' ~/.claude/projects/-home-pscicluna-GitHub-ampere/<session>/subagents/<agent>.jsonl` against the `5m` sibling) and record the answer in the re-evaluation's rule 3. A **Hand-off** report means: dispatch a successor on the same branch within the hour, same tier unless the remainder is mechanical. (4) **Fillers in this order as slots free** (all ruled 2026-09-22, texts in WORK_ITEMS.md): W5.25 (`halfcauchy` lowering, Sonnet S) first because the recommended shrinkage prior is reference-only on NUTS until it lands; W5.26 (test cost + CI modularity + the ungated `tests/interferometry` and `tests/astrometry` suites, Sonnet M — needs one CI run on `v2` to read); W5.27 (`shrinkage_horseshoe` rename with deprecated alias, Sonnet S, before W5.19); W5.28 (housekeeping II, Sonnet S, after W5.27); W5.29 (batched path drawing a context, Opus M); W5.21 and W5.24 as before; the rst doctest stays an unnumbered filler; then **wave 6** W5.15–W5.18; **W5.19 last**. The `Derived` parameter node is a Phase 6 design-horizon note (ruled), not scheduled. (5) **The remote.** On Peter's instruction (2026-09-22) master is pushed to **`origin/v2`** — the first push of any v2 code; `origin/master` stays at the legacy `b8e585b` until Phase 6 merges the line. `ci.yml` now triggers on pushes to `v2` as well as `master`. Re-push after each merge as `git push origin master:v2` **only when Peter says so** (ground rule 3 stands; the mirror is his command each time). Peter's untracked paper script under `examples/examples_paper/` stays untracked.

**Session record, 2026-09-22 02:20 → ~03:40 (the rulings session).** Found the 18 September gate log gone (the machine rebooted on 2026-09-21; the log lived in a session scratchpad under `/tmp`); re-launched the gate one leg at a time — dev green as recorded above, sbi launched 03:05. Briefed Peter on the six rulings with the code behind each; **rulings**: W5.10's joint-noise context refusal confirmed, the shared-context-per-group route left open; `halfcauchy` lowering scheduled (W5.25); the `Derived` node to Phase 6; the `tests/m2` cost accepted for this gate with the trim in W5.26; the helper renamed `shrinkage_horseshoe` with a deprecated alias (W5.27); the batched-path context item scheduled (W5.29). Peter's two questions answered and accepted: on test cost, the studies' sampling rows run in dev only and CI gains a suite-group matrix dimension (both W5.26; xdist and re-pinning out of scope); on the remote, master mirrored to `origin/v2` rather than pushed to `master`. **Found in passing**: `tests/interferometry` (22 rows) and `tests/astrometry` (40 rows) have been in no gate and no CI job since W4.4/W4.9 landed — W5.26 (2). Quick clear-ups landed in this commit: AGENTS.md/CLAUDE.md's phase paragraph (it still said Phase 4 was next), the two WORK_ITEMS.md text errors (W5.13's §13.15 → item 16, W5.22's nonexistent `test_provenance*.py`), the gate-log recipe moved to a durable path, `ci.yml`'s push trigger widened to `v2`, the rulings pasted into the W5.10 and W5.8 rows, the five items drafted. **Token audit** (Peter's small task): all 126 transcripts analysed — subagents run on the five-minute cache while main sessions have the hour, 16 % of agent cost since 15 September was cold restarts after test runs of ~10 min, and context size is the first-order cost (requests over 300 k carry 63 %); five rules adopted by Peter and written into `docs/orchestration.md` ("Re-evaluation"), the `work-item` skill ("Budget") and the standing dispatch paragraph — the soft lifetime cap with hand-off, the five-minute rule, the tier check on the next dispatch, design documents by section, batched verification. The sbi gate leg closed green (4046 passed / 101 skipped in 56 min 19 s; baseline 3900/96 in 46) and the torch leg was launched at 04:03 on `71c1638` (docs and `ci.yml` only since `264fbd4`). **The first CI run on `v2` (`35679246921`) was red in every job at the pixi setup step**: `pixi install --locked` reports the lock stale ("'torch' requires index pypi.org but the lock-file has download.pytorch.org") on pixi 0.68 and 0.81 alike, locally too; the lock is in fact current (a re-solve reproduces it byte for byte) and the check does not apply the per-package CPU index to the transitive `torch` requirement — two manifest forms tried, neither helps; `ci.yml` now installs `frozen` (all nine setup-pixi steps), actionlint clean, and W5.26 carries the proper fix as item (6). **The second CI run (`35684651060`, after the `frozen` fix) got past setup everywhere and found one real defect: the py311 leg's `tests/core/test_astropy_engines.py` errors at `emission.py`'s `.children` because arviz 1.x (which `ampere.results` is written against) needs Python 3.12, so 3.11 resolved arviz 0.23 — latent since W2.2. Peter ruled: raise the floor to 3.12 and make the freed leg 3.14** (decision-log row); landed as `requires-python >= 3.12`, `arviz >= 1`, classifiers, ruff `target-version = py312` (which made ruff ask for PEP 695 type parameters on `simulate.py`'s three generic helpers — applied, `Item`/`Result`), the pixi `py314` feature and `test-py314` environment replacing `py311`, the CI matrix `312/313/314`, AGENTS.md, this page and `install.rst`; the lock re-solved with every other environment's package set unchanged (checked); lint, format, typecheck (dev) and `tests/core/test_simulate.py` (128) green; the 3.14 environment installed locally (`--frozen`): the failing test 5 passed / 2 skipped, then `tests/core` + `tests/results` 1834 passed / 36 skipped in 13 min 49 s. **CI run `35684651060` completed**: 14 jobs green (lint, format, actionlint, docs, all five typecheck legs, minimal install, py312, py313, dev and torch suites), the characterisation job skipped by design, and three red — py311 (the floor, fixed above) and **two statistical rows that passed in the local gate on the same commit**: the sbi SBC p-value row (0.0198 against `> 0.05`) and the jax HSGP-under-NUTS width row (0.188 against 0.137, quarter pinned) — recorded as W5.26 (7) with recommendations; not fixed here. **Lesson**: a detached job's log under a session scratchpad in `/tmp` does not survive a reboot between sessions — anything the next session must read goes under `~/.cache/ampere-gates/` (or the repository).

**(Superseded 2026-09-22 — the gate log it points at was lost to a reboot; rulings and fillers now recorded above.)** **What a clean session does, in order (written 2026-09-18 ~06:30 by Fable at the close of the wave-4 session, which Peter closed as unusually expensive; everything below this paragraph is history).** (1) **Record the wave-4 gate.** It covers W5.11 + W5.10 + W5.8 and was launched detached on master `54cb2e4` at 06:27 on 2026-09-18, legs dev → sbi → torch → jax through `flock /tmp/ampere-gate.lock`, one summary line per leg appended to `/tmp/claude-1000/-home-pscicluna-GitHub-ampere/9e5649b6-9d5f-419e-8bd4-bd40fcbff315/scratchpad/wave4b-gate.log` (full output beside it as `wave4b-gate.log.<env>`; `=== ALL DONE` closes it; `pgrep -af wave4b-gate.sh` says whether it still runs). Baseline, the gate on `dd79aa0`: dev 2827/423 (20 min), sbi 3900/96 (46), torch 3766/230 (41), jax 3586/239 (47); expect every leg up by the three items' rows and the dev leg ≈ 10 min longer (W5.8's `tests/m2` rows). Copy the four counts into the W5.11, W5.10 and W5.8 status rows (each says "the merged-wave gate to be recorded"). If the log is missing after a reboot, re-run each leg without a result line, one at a time, as `flock /tmp/ampere-gate.lock pixi run -e <env> test-all`. A failure comes back to a cheap fix-up agent with the log excerpt. (2) **Six rulings pending for Peter**, all recorded in the rows' **For Peter** lists with recommendations: from W5.10 — a context on a joint noise group refused rather than guessed (recommend confirm); from W5.8 — `halfcauchy` in neither backend's lowering table (recommend a Sonnet filler: two lines per backend, a §3.2 row, a conformance row), a `Derived` parameter node (recommend a design-horizon note, not scheduled), `tests/m2` now ≈ 11 min per environment (recommend accept for this gate and fold a trim into the test-cost filler), the helper's name `regularised_horseshoe` against its gamma-tailed default (recommend keep). (3) **Dispatch wave 5: W5.12 ∥ W5.14** (both Opus; W5.12's text now carries the note on lifting `fit_population`'s hierarchical-prior refusal), prompts from the standing paragraph below plus each item's read-ranges, ownership and Accept list, base = master after the gate, fresh worktrees, staggered by an hour, **and preferably dispatched when no gate is running** — this session's two agents queued ≈ 2 h 20 m of their targeted runs behind the wave-4 gate's four legs. Then **wave 6** W5.15, W5.16, W5.17, W5.18 as slots free; **fillers** any time: W5.21 (the GRID gate, Sonnet), W5.24 (heteroscedastic joint noise, Opus M), the `halfcauchy` lowering rows (Sonnet, if ruled), one test-cost item trimming `tests/results/test_population.py` (≈ 8.6 min dev) and `tests/m2` (≈ 11 min) budgets (Sonnet), `SBIEngine.calibrate()` reseeding torch as `run()` does (Sonnet), a doctest over `docs/source/*.rst` (Sonnet), the native batched path drawing a context (Opus M, W5.10's carried item); **W5.19 last**. (4) **Housekeeping worth Sonnet items** (beside the fillers): `replace_observations` also drops `shared=`/`shared_label=`; `ampere.results.sbc` ranks posterior variables only; a `summarise`-side helper reporting `B`; `QuasisepGP.condition(at=...)` on a multi-axis container; `tests/` not an importable package; `_GriddedSolver` private but the extension point; `HilbertSpaceGP` forms two `(N, m)` blocks; `_concatenate` in `results/training.py` indexes the addition by the existing tree's groups; `TrainingSet.contexts` defaulting to `()`; `with_shrinkage` rebuilding the parameter set through a `__dict__` write; WORK_ITEMS.md's W5.13 text cites §13.15 (landed as 16) and W5.22's a `test_provenance*.py` that does not exist. (5) Nothing is pushed (origin at `b8e585b`); Peter's untracked paper script under `examples/examples_paper/` stays; no branch but master exists.

**Session record, 2026-09-17 23:50 → 2026-09-18 06:30 (the wave-4 session).** Gated master `dd79aa0` (green, recorded in W5.22's row); took Peter's six rulings on W5.7 and W5.22 (all confirmed as recommended, the two future-need notes recorded); dispatched W5.11→W5.10 (one agent, two sequenced branches) at 00:03 and W5.8 at 01:00 on Peter's go; deleted 153 merged branches on his instruction; reviewed all three items on Fable's second pass — W5.11 and W5.8 with no fixes, W5.10 with one (the context prior did not enter the artefact cache key; fixed on the branch as `artefact_key(context=)`, every pre-W5.10 digest unchanged); Peter merged W5.11 (`c81f975`), W5.10 (`d48c291`), W5.8 (`2a1f596`); all six rows pasted; the merged-wave gate launched. Agent usage: W5.11 + W5.10 ≈ 566 k tokens / 462 tool uses / 2 h 38 min; W5.8 ≈ 505 k / 89 / 1 h 40 min (both killed once by the session limit at ~03:50, reset 04:40, resumed by `SendMessage`, nothing lost — W5.8's two uncommitted edits preserved as a WIP commit first). **Lessons**: (a) a `Monitor` built on `tail -f` of the gate log delivered nothing — a 30 s poll loop that prints new summary lines works, and its expiry is a usable timer; (b) a merge message with double quotes inside `-m "..."` breaks the shell (`merge: lines - not something we can merge`) — hand Peter `git merge --no-ff <branch> -F <scratchpad message file>`; (c) dispatching under a running gate queues every targeted run behind up to four legs — dispatch with the lock free, or accept the wait; (d) the harness's `cd` into a worktree persisted into the orchestrator's shell once (as memory warns) — every command starts with `cd` to the main checkout; (e) an agent asked to add its own test row for a claim it stated ("the original kernel is unchanged") had not — verify such claims directly at review, it costs one command.

**W5.8 merged by Peter at `2a1f596` (2026-09-18 ~06:00); both rows pasted; the last worktree removed; wave 4 complete.** **(Record of the W5.8 review, superseded):** (branch `w5.8-m2-many-lines`, head `46b49b6` — master merged at `d48c291`, after W5.10, the `__all__` collision resolved with both entries; lint, format-check and the import sweep re-verified by the orchestrator on that head; 17 files, +2771/−26 against `dd79aa0`). Fable's review: read as its row says, `with_shrinkage`'s functional claim verified directly; the orchestrator's re-runs through the lock: dev `test_parameter.py` 158 in 3.6 s, the driver/arm rows of `test_many_lines.py` 17 in 2 min 14 s; no fixes needed. **Order**: Peter merges W5.10 first; the orchestrator then tells the W5.8 agent (`SendMessage`, it is alive) to `git merge master` — both items append to `ampere/core/__init__.py`'s `__all__`, so the agent resolves that collision (keep both) and re-runs lint/format — and reports its new head; **then Peter runs, from the main checkout**: `git merge --no-ff w5.8-m2-many-lines` with the message below. Then the orchestrator pastes the two rows with the merge hash, removes both agent worktrees, and launches the wave-4 merged gate (all four environments; the dev leg now carries `tests/m2`'s extra ≈ 10 min). Four rulings for Peter are in the status row's **For Peter** list.

*Merge message:*

```
Merge branch 'w5.8-m2-many-lines'

W5.8: the M2 extension "many lines / one band" -- five narrow lines in
one band plus a smooth continuum error, comparing stationary Matern,
W5.7's warped Matern and W4.5's Sum on bias, calibration and
localisation with pinned margins (the stationary kernel is the first
flexible likelihood this study requires to fail) -- and the sparsity
guard for summed noise components: ampere.core.regularised_horseshoe
expressed with HierarchicalPrior, with_shrinkage as the one kernel hook,
the non-centred note in lowering.md section 3. Opus-authored,
Fable-reviewed; no fixes needed.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EKDxVWwByo73eerbrHdD7H
```

*Status row (WORK_ITEMS.md, after W5.10's):*

| W5.8 | merged 2026-09-18 at `MERGE_HASH` (Opus-authored, Fable-reviewed as the second pass — the horseshoe's global-local chain as three `HierarchicalPrior` levels, the `with_shrinkage` hook (functional; verified directly: the original kernel's spec hash and parameter names unchanged, the shrunk kernel's hash moved, the scale levels registered ahead of the amplitudes, `terms` and `FAMILY` preserved), the pinned margins against the measured values, the SBC contrast re-pinned as the visibility precedent does, and the driver's own rows read; the orchestrator's re-runs on the branch through the lock: dev `tests/core/test_parameter.py` 158 passed in 3.6 s, the driver/arm rows of `tests/m2/test_many_lines.py` 17 passed in 2 min 14 s; **no fixes needed**; merged by Peter; the agent's runs: dev `tests/m2` 112/32 in 11 min 24 s, `test_many_lines.py` 29/2 in 4 min 41 s, `test_many_lines_calibration.py` 6/1 in 2 min 49 s, torch NUTS-over-the-knots 2/2 in 4 min 51 s, jax 2/2 in 1 min 1 s, lint/format/typecheck (dev, torch, jax) clean, import sweep 86/4, docs build clean; the merged-wave gate to be recorded with W5.11 and W5.10; the agent: ≈ 503 k tokens / 82 tool uses over two segments, killed once by the session limit with two edits preserved as a WIP commit). Landed: `ampere.core.regularised_horseshoe(amplitudes, *, prefix, global_scale, tail, unit)` — `τ ~ C⁺(0, τ₀)`, `s_j | τ` a `gamma(½, scale=τ)` (`tail="regularised"`, the default, which lowers) or `C⁺(0, τ)` (`tail="cauchy"`, the plain horseshoe, which does not lower on torch/jax yet), `a_j | s_j ~ N⁺(0, s_j)`, all with `Log` bijections; `ampere.core.with_shrinkage(kernel, declaration)`, the one hook, appended to `kernels.py`; the `many_lines` scenario (kept out of `SCENARIOS`, so the milestone's four are untouched), the `warped` and `sum` arms in `build_kernel`, `examples/m2_misspecification/many_lines.py` and the driver's `--scenario many_lines --kernel {matern32,warped,sum}` (a trailing-block `KeyError` on the warped arm fixed in scope); `tests/m2/test_many_lines.py` (29 rows) and `test_many_lines_calibration.py` (SBC with a parameter-free `LineForestError` step in the simulating problem), the backend-agreement row for the warped arm; `likelihoods.md` §6 (line 889's promise discharged) and §13, `parameters.md` §9, `lowering.md` §3.2.1, `m2_misspecification.rst` (two sections) and `kernels.rst`. Measured at 200 points, bias in posterior widths: standard 35.70 (1/4 covered), stationary Matérn 2.84 (3/4; pinned ≥ 2.0 and > 1.5), warped 0.92 (4/4), `Sum` 0.78 (4/4; each pinned ≤ 1.5 and ≥ 2.0× better than stationary, measured 3.1× and 3.7×); all three localise inside the band (in-band contrast 2.76×, 1.90×, 3.02×, pinned ≥ 1.4×); the horseshoe row `min(a)/max(a)` 0.254 → 0.087 (2.93×, pinned ≥ 1.3) and mass below a tenth 0.257 → 0.537 (2.09×, pinned ≥ 1.2), thresholds a third below the worst of three seeds; SBC at nominal 0.90: standard [0.00, 0.80], warped [0.80, 0.90], gap pinned ≥ 0.4; the warp's fitted increments give an effective length scale of 0.028 µm under the smooth error and 0.0009 µm under the forest, the sign change at the band edge. **For Peter**: (1) `halfcauchy` is in neither backend's lowering table, so the plain-horseshoe tail and the global level are reference-only on NUTS — two lines per backend plus a §3.2 row and a conformance row, a Sonnet filler; (2) a `Derived` parameter node (a pure function of other parameters) would make P&V's slab declarable and let `WarpedKernel` declare its non-centring — a design question, not scheduled; (3) `tests/m2` in dev now costs 11 min 24 s per environment (was ≈ 100 s): `many_lines.SHRINKAGE_EMCEE` 3.5 min, the four arms 2.4 min, the calibration row 2.8 min, each set against a measured scatter — accept, or name the lever; (4) the helper is named `regularised_horseshoe` while its default tail is a gamma variant of the horseshoe rather than Piironen & Vehtari's slab — the docstring says so; keep the name, or rename. Carried: `with_shrinkage` rebuilds the parameter set through `copy.copy` and a `__dict__` write rather than a constructor path; `arviz_base`'s chain-longer-than-draw warning on every emcee run (pre-existing). |

*Decision-log row (DEVELOPMENT_PLAN.md §2, after the W5.10 row):*

| The sparsity guard on a sum of noise components, and the M2 scenario that needs two length scales (W5.8) | **`ampere.core.regularised_horseshoe` is the recommended prior for any `Sum` of noise terms, and `ampere.core.with_shrinkage` is the one hook that puts it on a kernel** (merged 2026-09-18, `MERGE_HASH`; Opus-authored, Fable-reviewed). The declaration is the horseshoe's global-local chain exactly — `τ ~ C⁺(0, τ₀)`, `s_j | τ ~ C⁺(0, τ)`, `a_j | s_j ~ N⁺(0, s_j)` — and the multiplicative `s_j = τ λ_j` is written as **one** declaration rather than a product, which it may be because the half-Cauchy is a scale family; that is what lets the whole prior be expressed with `HierarchicalPrior`, which references a parameter by name and has no product node, with **no change to any existing class**. **The hook could not be avoided and the reason is structural**: a `ParameterSet` refuses, on each registration, a reference not already declared — which is what stops a dangling reference reaching a sampler — and a kernel registers a summand's `amplitude` in its own `__init__` before anything a horseshoe would give it to reference, so the scale levels must be registered ahead of the amplitudes and only something that rebuilds the whole set can do that. `with_shrinkage` is **functional** (the kernel it is given is unchanged — verified at review: the original's spec hash and names are untouched and the shrunk kernel's hash moves), `_Composite.__init__` is untouched, and a kernel built without it has exactly the parameters, spec and hash it had before; `FAMILY`, `terms`, capabilities and axes are untouched, so it lowers unchanged and the extra names never reach a child because `_child_values` selects on the `label.` prefix. **What is *not* Piironen & Vehtari's**: their slab, `λ̃_j² = c²λ_j²/(c² + τ²λ_j²)`, is a deterministic function of two sampled parameters and §4.1 declares parameters and priors, not deterministic nodes — so `tail="regularised"` (the default) gives the declarable form of the same guard, a `gamma(a=½, scale=τ)` local level that keeps the horseshoe's `s^{-½}` spike and replaces the Cauchy tail with an exponential one, and a `Derived` parameter node is recorded as what would close the gap. It also lowers, where the plain horseshoe does not: **`halfcauchy` is in neither backend's §3.2 table**, so `tail="cauchy"` and the global level need a `register_lowering` row, recorded in `lowering.md` §3.2.1 along with the non-centred parameterisation NUTS wants and why spike-and-slab stays out (§12 Q1: its indicator is discrete). **Validated by the M2 extension the plan's Phase-5 bullet names**: `many_lines`, five narrow lines confined to 0.860–0.870 µm plus a smooth continuum error a hundred times broader — kept **out** of `SCENARIOS`, so `run_study` still defaults to the four and the milestone's figures, tables and CI session are unchanged. Measured at 200 points: standard 35.70 posterior widths (1/4 covered), stationary Matérn **2.84 (3/4)** — the first place in this study a flexible likelihood is required to *fail* M2's own 1.5 threshold, and it fails for a structural reason, the kernel being the wrong *shape* — W5.7's warped Matérn 0.92 (4/4) and W4.5's `Sum` 0.78 (4/4), all three localising inside the line band. Every threshold is a margin, not a number (W4.5's `_period_margin` precedent), and the shrinkage thresholds sit a third below the worst of three run seeds because the horseshoe's three levels give the posterior a funnel an ensemble sampler explores unevenly. SBC over refits with the deviation injected pins the contrast the visibility precedent pins (standard 0.00/0.80 against warped 0.80/0.90 at nominal 0.90), not an absolute coverage, because at ten refits an exactly calibrated fit returns 8/10 more than a quarter of the time. Cross-backend: torch 0.130 and jax 0.085 on a median against a 0.5 tolerance, with NUTS reaching all six warp variables through `ampere.core.realise`. `likelihoods.md` §6 (discharging its line 889 promise) and §13, `parameters.md` §9, `lowering.md` §3 amended. Conformance untouched and green. |

**W5.10 merged by Peter at `d48c291` (2026-09-18 ~05:40); both rows pasted; the agent's worktree removed; the W5.8 agent told to merge master.** **(Record of the W5.10 review, superseded):** (branch `w5.10-context-amortisation`, head `f17084b`, master merged at `33ac6e7`, so the merge is clean; 15 files, +2075/−88 against master). Fable's review: read as its row says; one fix at review (the context prior joining the artefact cache key), landed as `71bfb49` and re-checked; the orchestrator's re-runs through the lock: dev 242 passed in 46 s and sbi `test_sbi.py` 179/2 in 5 min 20 s before the fix, dev `test_artefacts.py` 63/3 in 4 s after it; the agent's post-merge sbi run 248/2 in 6 min 14 s. **Peter runs, from the main checkout**: `git merge --no-ff w5.10-context-amortisation` with the message below. Then the orchestrator pastes the two rows below with the merge hash, removes the agent's worktree (`git worktree remove --force --force .claude/worktrees/agent-aaff3f4f29f6db1a1`), and tells the W5.8 agent to merge master again before reporting (W5.10 touches nothing W5.8 owns).

*Merge message:*

```
Merge branch 'w5.10-context-amortisation'

W5.10: amortisation over the observation context -- the ContextPrior
protocol (ScaledSigma, SigmaArchive, SignalToNoise) drawn per simulation
on its own sub-stream by simulate_many(context=...), the drawn sigma
replacing the observed container's for that draw so every family's
sample sees it, recorded on every Simulation, in the training set and in
the SBI run's attrs, and an ingredient of the artefact cache key; FiLM
as the opt-in second route; calibrate(context=...) as the coverage
check. No layout hash and no schema version moved. Opus-authored,
Fable-reviewed; one fix at review (the prior joining the cache key).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EKDxVWwByo73eerbrHdD7H
```

*Status row (WORK_ITEMS.md, after W5.11's):*

| W5.10 | merged 2026-09-18 at `MERGE_HASH` (Opus-authored, Fable-reviewed as the second pass — `ContextPrior` and the three shipped priors' draws, `Dataset.contextual_observed` as the single route of a context's σ into every family's `sample`, the `"<stream>.context"` sub-stream spawned by index (so every pre-existing budget's θ and noise are bit-for-bit unchanged), the three refusals by name, the FiLM branch's zero-initialised last layer and the pinned calibration inequalities read; **one fix at review**: the context prior did not enter the artefact cache key, so a network trained without a context would have been served for a context-amortised run with the same settings (§7's stale-artefact trap) — fixed as `artefact_key(context=<digest>)`, omitted when `None` so every pre-W5.10 digest is unchanged (verified byte-for-byte against master's own `artefacts.py`, `3af107dc…` either side; W3.12's pinned NPE digest untouched), with 7 store rows and 3 engine rows; the orchestrator's re-runs on the branch: dev `test_encoding.py` + `test_simulate.py` + `test_training.py` 242 passed in 46 s, sbi `test_sbi.py` 179/2 in 5 min 20 s (pre-fix), dev `test_artefacts.py` 63/3 in 4 s (post-fix); merged by Peter; the agent's runs: dev 242, sbi `test_sbi.py` + `test_artefacts.py` 248/2 in 6 min 14 s, torch `test_simulate.py` 128 in 56 s, lint/format/typecheck (dev, sbi, torch) clean, import sweep 86/4; the merged-wave gate to be recorded with W5.8; the agent, W5.11 and W5.10 together over two segments: ≈ 566 k tokens / 462 tool uses / 2 h 38 min, most of it queued behind the wave-4 gate's four legs on the lock). Landed: `ampere.core.simulate.ObservationContext` (per-dataset σ arrays plus a JSON-safe record), the structural `ContextPrior` (`draw(rng, observed)`, `describe()`), `ScaledSigma`, `SigmaArchive`, `SignalToNoise`; `Dataset.contextual_observed(sigma)` and `draw_observation(..., sigma=)` — no `LikelihoodFamily.sample` signature moved; `Simulation.context` (failures included); `simulate_many(context=...)` drawing one context per simulation on its own sub-stream, materialised a chunk at a time; the training set's optional `context` group and `ampere_simulation_context`, append refusing a context mismatch in both directions; `SBIEngine(context=...)` with `ampere_sbi_context`/`ampere_sbi_context_hash` and the prior's digest in the cache key; `calibrate(context=...)` inheriting the run's prior by default; FiLM (`embedding={"type": "set", "film": True}`), off by default, the identity before training; `inference.md` §12/§13, `encoding.md` §3/§8, `results.md` §9/§11 amended, `sbi.rst` gains "Amortising over the observation context". `PROVENANCE_SCHEMA_VERSION` stays 8 and `TRAINING_SET_SCHEMA_VERSION` 1 (engine-specific extras; an optional group). Measured: a 1500-draw NPE under `ScaledSigma(0.5, 2.0)` — TARP area-to-curve −0.007 at a ×1.5 rescale the prior covers, −0.046 at ×12 it does not, pinned as inequalities at about half the separation; the whole of `test_sbi.py` in sbi in 6 min, so no marker. **For Peter**: a context on a problem with a joint noise group (W5.9) is refused by name rather than guessed (one shared context per group from its first dataset is the implementable alternative) — confirm. Carried: the native batched path refuses a context by name (a per-draw σ does not reach the realisation; a real follow-on item); `SignalToNoise` shapes σ from the *observed* magnitudes because a context is drawn before the forward model runs (documented; a prior tracking each draw's own signal needs `draw` to receive the predicted containers — a protocol change); `_concatenate` in `training.py` indexes the addition by the existing tree's groups (a bare `KeyError` or a silent drop for any future optional group); `TrainingSet.contexts` defaults to `()`, so "written before W5.10" and "written without a context" are indistinguishable in Python (the file distinguishes them); sbi 0.27's outlier warning on the set layout under a wide prior (`z_score_x` guidance for the tutorial); a pre-existing wrong stream name in `_calibration_batch` fixed on the branch because W5.10 made it load-bearing. |

*Decision-log row (DEVELOPMENT_PLAN.md §2, after the W5.11 row):*

| **Amortisation over the observation context: the `ContextPrior` (W5.10)** | **`simulate_many(context=...)`'s reserved slot is filled, and no layout hash moved to fill it** (merged 2026-09-18, `MERGE_HASH`; Opus-authored, Fable-reviewed; one fix at review). A budget drawn the ordinary way uses the observed container's own uncertainties, so the posterior it trains is amortised over noise *realisations* but not over noise *levels*. `ampere.core.simulate.ContextPrior` — `draw(rng, observed) -> ObservationContext` and `describe()`, structural like `Executor` — is what varies them, with three shipped instances answering the three ways an uncertainty pattern is actually known: `ScaledSigma` (scaled copies of the observed pattern), `SigmaArchive` (real error arrays) and `SignalToNoise` (a parametric S/N model, the one needing no observed σ at all). **The σ reaches exactly one place**: `Dataset.contextual_observed` replaces that dataset's observed container for that draw, so every family's `sample` receives `NoiseParams` built from the context and the drawn observation carries its own error bars — a draw at a different noise *level*, not a rescaling applied afterwards, and **no `LikelihoodFamily.sample` signature moved**. That is also why `encoding.md` needed no new column and **every layout hash is unchanged**: the packing already carries each sample's `log σ` and whitened value, so a network reading `unpack` sees the context of the observation it is conditioned on, and the reserved `context` group stays width 0 for a context that is genuinely not already a column. The opt-in second route is FiLM — a per-dataset conditioning vector from the masked mean and spread of `log σ` and `log N_valid`, zero-initialised so that switching it on is the identity before the first gradient step. **Contexts are drawn per simulation on their own `"<stream>.context"` sub-stream, spawned by index** (`inference.md` §12): a budget's θ and its noise are bit-for-bit what they were before the item existed, and draw *i*'s context is independent of the chunking — they are materialised a chunk at a time so a budget larger than memory never holds them all. Recorded on every `Simulation` (failures included), in the batch's `provenance['simulation_context']`, in a training set's optional `context` group and root attrs, and in an SBI run's `ampere_sbi_context`/`ampere_sbi_context_hash`; the σ arrays are **not** stored twice, because a drawn observation carries them. **The prior is also an ingredient of the artefact cache key** (`artefact_key(context=…)`, `results.md` §9), and that is not book-keeping: a budget drawn under a context prior trains a different network from one drawn at the observed uncertainties, and two priors covering different ranges train two more, so without it all three computed the same key and the store served whichever was trained first — §7's stale artefact arriving by the one door the key exists to shut, and something the recorded attrs can only diagnose after the fact (found at review). It is the prior's digest rather than its description (an archive of real error arrays does not belong in every sidecar), it is `None` for a budget drawn without one, and it is omitted from `ingredients()` rather than written as `null`, so **every digest minted before W5.10 is unchanged** — verified byte-for-byte against master's own `artefacts.py` (`3af107dc1797effaf1cc51c626312838` either side), and W3.12's pinned `_BASE_COMMIT_NPE_DIGEST` still holds untouched. **SBC per observation is the check that the prior covered the observation at hand** and `calibrate(context=...)` is how it is asked: inheriting the run's prior by default, taking one of its own otherwise, and recording which distribution each verdict is about. Measured on a 1500-draw NPE under `ScaledSigma(0.5, 2.0)`: TARP area-to-curve `-0.007` at a ×1.5 rescale the prior covers and `-0.046` at a ×12 it does not, pinned as inequalities at about half the separation. Three refusals by name rather than guesses: `observe=False` (the context *is* the level the observations are drawn at), `native=True` (the native path draws its noise from the realisation's own σ, which a per-draw container σ does not reach — the loop runs instead and the provenance says so), and a problem with a **joint noise group** (a group's σ is read from the first of its datasets and its channels are drawn in one correlated call, so what a per-dataset context means for the cross-covariance is a design question, not a detail). `SignalToNoise` shapes its σ from the **observed** container's magnitudes, because a context is drawn before the forward model runs: under `reference="values"` every draw gets the observation's own fractional-error shape at one drawn S/N, not its own simulated signal's — stated in the docstring; a prior that tracked each draw would need `draw` to receive the predicted containers, which is a protocol change and a later item. `PROVENANCE_SCHEMA_VERSION` unchanged at 8 and `TRAINING_SET_SCHEMA_VERSION` at 1: the context attrs are engine-specific extras and the `context` group is optional, exactly as `observations` was at W2.8. |

**W5.11 merged by Peter at `c81f975` (2026-09-18 ~04:00); both rows pasted. Both agents were killed by the session limit at ~03:50 (reset 04:40); W5.10's cache-key fix was already committed (`71bfb49`), W5.8's two uncommitted test edits preserved as WIP `8c78beb`; both resumed by `SendMessage` at 04:45. Peter's plan: close the session after this round of agents finishes.** **(Record of the W5.11 review, superseded):** (branch `w5.11-axis-identity`, head `4c3b1a2`, base `dd79aa0` with master merged to `fb18ef8`, so the merge is clean; 7 files, +674/−34; the same agent then did W5.10 on a branch from this head, so merging W5.11 first is the forced order). Fable's review: read as its row says; the orchestrator's re-run on the branch through the lock: dev `test_encoding.py` + `test_simulate.py` + `test_training.py` 242 passed in 46 s, sbi `test_sbi.py` 179/2 in 5 min 20 s; no fixes needed. **Peter runs, from the main checkout**: `git merge --no-ff w5.11-axis-identity` with the message below. Then the orchestrator pastes the two rows below with the merge hash and tells the W5.8 agent to merge master. **W5.10 follows once its one review fix lands** (the context prior entering the artefact key; the agent is on it).

*Merge message:*

```
Merge branch 'w5.11-axis-identity'

W5.11: the encoding's axis identity -- one small integer per coordinate
column, taken from the axis's unit through astropy's physical type, as
a tenth column group of the packing; ENCODING_VERSION 1 -> 2, every
layout hash moves once, and a pre-W5.11 layout or training set is
refused by name at both doors. Opus-authored, Fable-reviewed; no fixes
needed.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01EKDxVWwByo73eerbrHdD7H
```

*Status row (WORK_ITEMS.md, after W5.22's):*

| W5.11 | merged 2026-09-18 at `MERGE_HASH` (Opus-authored, Fable-reviewed as the second pass — the thirteen-entry code table, the unit-to-code rule through `astropy.units.get_physical_type` with the one declared exception (spatial frequency, named by a kind putting `rad**-1` in its `AxisSpec.equivalent_units`, coded 8 whether the axis is given in wavelengths or in inverse radians, while a `u` in metres is honestly a length), `ENCODING_VERSION` in the hashed layout record, and the shared by-name refusal `axis_identity_complaint` at both doors read; the orchestrator's re-run of `tests/core/test_encoding.py` + `test_simulate.py` + `tests/results/test_training.py` in dev on the branch: 242 passed in 46 s (and `test_sbi.py` in sbi 179/2 in 5 min 20 s); **no fixes needed**; merged by Peter; the agent's runs: dev targeted 242 passed, sbi `tests/inference/test_sbi.py` 179/2 in 6 min 17 s, lint/format/typecheck (dev, sbi, torch) clean, import sweep 86/4; the merged-wave gate to be recorded with W5.10 and W5.8; the agent's usage is W5.10's row's — one agent did both items in sequence). Landed: `AXIS_TYPE_CODES` (contract: `absent` 0 for a padded column, `unknown` 1, then dimensionless, length, frequency, energy, time, angle, spatial frequency, wavenumber, speed, temperature, mass — a code may be appended, never renumbered), `AXIS_TYPE_NAMES`, `axis_type_code(unit, spec=)`; `DatasetLayout.axis_codes`/`axis_type_names` in the layout, its dict form, its hash and `compare`'s report; the `axis_identity` column group (`encoding.md` §3 group 4, width A, the code on every row of its dataset's block) exposed by `unpack` on `Unpacked` and every `DatasetView` and carried by `features`, so no embedding wrapper changed; `ENCODING_VERSION` 2 with `EncodingLayout.from_dict` and `append_training_set` refusing a version-1 record by name through one message. Measured: the module docstring's one-dataset `Spectrum` layout `e73db8a8032273f4c511142f1459c51c` (17 columns, version 1, computed from `dd79aa0`'s own code) → `822f4c900af7a854488b2044db598e26` (18 columns, version 2). Deliberately narrower than what it closes: the code names a physical type, not a role — two `length` axes in one collection (a wavelength and a baseline) still share a code, and a kind wanting them distinguished needs a new code, not a new mechanism. |

*Decision-log row (DEVELOPMENT_PLAN.md §2, after the W5.22 row):*

| **The encoding's axis identity: a physical-type code per coordinate column (W5.11, D7)** | **`encoding.md` §3 gains a tenth column group, `axis_identity`, and every layout hash moves once** (merged 2026-09-18, `MERGE_HASH`; Opus-authored, Fable-reviewed). §9 item 6 recorded that the packing aligns axes by *position* — column `k` is whichever axis a kind declares in position `k` — so a collection mixing an `Image` (column 0 is `x`, an angle on the sky) with a `VisibilitySet` (column 0 is `u`, a spatial frequency) showed a network two unrelated quantities in one input slot. The lift is **one small integer per coordinate column, taken from the axis's unit** through `astropy.units.get_physical_type`, which is the one statement of what a coordinate is that every container already carries: a `Spectrum` in `um` and one in `Hz` hold different quantities in the same column and the codes now say so. The thirteen-entry table (`AXIS_TYPE_CODES`) is **contract** — the numbers are frozen into every layout's hash and every trained network's input, so a code may be added as the next free integer but never renumbered or reused — and `0` (`absent`, the padded column of a dataset with fewer axes than the widest) is deliberately distinct from `1` (`unknown`, an axis whose physical type the table cannot name). The **one** quantity astropy cannot name is `spatial frequency`: `u` in wavelengths is a baseline over a wavelength and reads `dimensionless`, `u` in `rad**-1` reads `unknown` (a radian is an *angle* to astropy, and the reciprocal of an angle has no name), so a container kind declares it by putting `rad**-1` in its `AxisSpec.equivalent_units` and an axis matching either spelling is coded 8 — while a `u` given in metres is a baseline **length** and is coded 3, which is the honest answer. `DatasetLayout.axis_codes` puts the codes in the layout and therefore in the hash; `unpack` exposes the group on `Unpacked` and every `DatasetView` and `features` carries it, so no embedding wrapper needed changing. **`ENCODING_VERSION` 1 → 2, and every layout hash written under version 1 moves exactly once** — measured on the module docstring's one-dataset `Spectrum` layout: `e73db8a8032273f4c511142f1459c51c` (17 columns, version 1) → `822f4c900af7a854488b2044db598e26` (18 columns, version 2), the old value taken from `dd79aa0`'s own code rather than reconstructed. The version is what lets both doors refuse a pre-W5.11 record **by name** — `EncodingLayout.from_dict` and `ampere.results.training.append_training_set`, sharing one message through `axis_identity_complaint` — rather than report a hash that does not match. Deliberately left open, and narrower than what was closed: the codes name a *physical type*, not a role, so two `length` axes of one collection (a wavelength and a baseline) still share a code, and a kind that wants them distinguished needs a new code rather than a new mechanism. |

**State at 2026-09-17 23:55 (the wave-4 preparation session; written by Fable).** Found: master clean at `dd79aa0` apart from Peter's untracked paper script; no agents, no worktrees, machine idle (8 GB free, 175 GB disk). Done: (1) **the gate on master launched detached** at 23:51 on `dd79aa0` — legs dev → sbi → torch → jax through `flock /tmp/ampere-gate.lock`, one summary line per leg appended to `/tmp/claude-1000/-home-pscicluna-GitHub-ampere/9e5649b6-9d5f-419e-8bd4-bd40fcbff315/scratchpad/wave4-gate.log` (full output beside it as `wave4-gate.log.<env>`; `=== ALL DONE` closes it; `pgrep -af wave4-gate.sh` says whether it still runs). Expect it to finish about 02:30. **Record the four counts in W5.22's status row** (baseline on `8e10b55`: dev 2818/422, sbi 3891/95, torch 3757/229, jax 3577/238); a failure comes back to a cheap fix-up agent with the log excerpt. If the log is missing after a reboot, re-run the legs without a result line one at a time as `flock /tmp/ampere-gate.lock pixi run -e <env> test-all`. (2) **The two wave-4 prompts are drafted** in the same scratchpad as `w5.10.prompt.md` (W5.11 first on `w5.11-axis-identity`, then W5.10 on `w5.10-context-amortisation` branched from W5.11's head — two branches, two rows, one agent, so every layout hash moves once per D7 and W5.11 is mergeable alone if the agent is interrupted) and `w5.8.prompt.md` (`w5.8-m2-many-lines`), each the standing paragraph (`standing.md`, updated with the 2026-09-17 lessons: pytest runs launched detached behind the lock because a gate leg may hold it for 50 minutes; `git merge master` before reporting) plus the item's read-ranges, ownership and Accept list; base `dd79aa0`. If the scratchpad is gone, the prompts are reproducible from the standing paragraph in this section plus these facts: W5.11/W5.10 read `encoding.py` (`DatasetLayout` 164–251, `EncodingLayout` 251–537, `unpack` 690–751, `_hash_of` 1224), `dataset.py` (`Simulation` 835, `simulate_many` 2994–3202 with the `context=` refusal documented at 3127–3137, `_simulate_chunks` 3202, `_native_batch` 3371), `_sbi.py` (`__init__` 1416 with the refusal at 1501, `run` 1575, `calibrate` 1853, `_simulate_round` 2629, `_attrs` 2813, embeddings 610–1170), `results/training.py` (`append_training_set` 401, the version check 999), `encoding.md` §3/§5/§7/§8/§9 item 6, `inference.md` §12/§13, `docs/source/sbi.rst`; they own those files plus `calibration.py`, `provenance.py`, `tests/core/test_encoding*.py`, `tests/inference/test_sbi.py`, `tests/results/test_training.py`; environments dev + sbi (+ torch for native batched simulation). W5.8 reads `examples/m2_misspecification/` (`generators.py` `SCENARIOS` 169, `study.py` `build_kernel` 353, `fringing.py` as the extension pattern), `tests/m2/` (`test_fringing_kernel.py` `_period_margin` 158 as the pinned-margin precedent, `test_visibility_calibration.py` `run_sbc` 305 as the SBC precedent, `conftest.py`'s `m2_full` skip 97), `kernels.py` `WarpedKernel` 2052 and `Sum` 1673 (read-only), `parameter.py` `HierarchicalPrior` 514 (the regularised horseshoe as a helper appended in a delimited block), `likelihoods.md` §6 (line 889 promises the horseshoe) and §13, `parameters.md` §9, `lowering.md` §3/§8/§12 Q1, `docs/source/m2_misspecification.rst`; it owns the study, `tests/m2`, the helper, those contract sections; environments dev + torch + jax; it never touches `likelihood.py`. Both Opus; the trailer names Claude Opus 5. (3) **Not done, deliberately**: nothing dispatched — "prepare" was the instruction, it is midnight, and the gate holds the lock for the next ~2.5 h (an agent's first targeted run would queue behind it). **Wave 4 dispatched on Peter's go (2026-09-18)**: the W5.11→W5.10 agent at 00:03 (worktree `.claude/worktrees/agent-aaff3f4f29f6db1a1`; W5.11 finished on `w5.11-axis-identity` within the first half hour and the agent moved to `w5.10-context-amortisation`), the W5.8 agent at 01:00 (worktree `.claude/worktrees/agent-aec10ef60e94189d8`, branch `w5.8-m2-many-lines`), both Opus, both on base `dd79aa0`, staggered by an hour per `docs/orchestration.md`. **The gate on `dd79aa0` finished 02:28 on 2026-09-18, all four legs green — dev 2827/423 (20 min), sbi 3900/96 (46), torch 3766/230 (41), jax 3586/239 (47) — recorded in W5.22's row; master is fully gated.** **Next**: when each agent reports, review on Fable's second pass (read the mathematics, re-run the branch's rows through the lock, detached), draft the merge message and both rows here, and ask Peter to merge — W5.11 first, then W5.10, then W5.8 (W5.8 touches nothing W5.10 owns, so the order between the two branches is free; W5.11 before W5.10 is forced). Tell the other agent to merge master after each merge. After all three merge, one four-environment gate. If an agent dies (rate limit, memory guard), `SendMessage` to its id resumes it from its transcript; preserve any uncommitted worktree edit as a WIP commit on its branch first.

**Previous head (2026-09-17 ~10:00, close of the wave-3 session; superseded above).** (1) **Gate master first.** Launch the four-environment gate detached on master (`b16607f` or later), legs dev → sbi → torch → jax through `flock /tmp/ampere-gate.lock`, one summary line per leg (the script pattern is in the session record below; `wave3-gate.sh` in the previous session's scratchpad may be gone — rewrite it: `for env in dev sbi torch jax; do flock /tmp/ampere-gate.lock pixi run -e $env test-all > log.$env; done`). It covers W5.22 (schema 8, the `population_full` marker) on top of the gated `8e10b55`; record the four counts in W5.22's row ("the next merged-master gate to be recorded"). Baseline, the gate on `8e10b55`: dev 2818/422, sbi 3891/95, torch 3757/229, jax 3577/238; expect dev ≈ 25 min, sbi ≈ 50, torch ≈ 45, jax ≈ 35, and the dev/sbi legs a few minutes shorter now that the 200-object archive is marked. **Do not wait on it with a foreground or background `sleep` loop whose lifetime exceeds ten minutes** — the harness's low-memory guard killed two such waiters on 2026-09-17 while the detached gate itself finished; check the log at the next natural event instead. (2) **Dispatch wave 4: W5.10 ∥ W5.8** (both Opus; W5.11 rides in W5.10's wave per D7 so every layout hash moves once — give it to the W5.10 agent as a second, sequenced item on the same branch, or a third agent after W5.8 reports). Prompts follow this session's pattern (the session record names the scratchpad drafts, which are reproducible from the standing paragraph further down plus each item's read-ranges, ownership and Accept list): base = master after the gate; fresh worktrees (`isolation: "worktree"`; the harness creates them at the session-start commit, so the ancestor/descendant recipe is mandatory); each agent installs its own environments in the background (`dev` + `sbi` for W5.10/W5.11 — the encoding and the SBI engine; `dev` + `torch` + `jax` for W5.8 — the M2 study runs on every backend); agents run only quick checks and targeted files through the flock, never `test-all`; merges are Peter's (draft the message and both rows into this section, commit, ask him); after both merge, one gate. **Ownership sketch**: W5.10/W5.11 own `ampere/core/encoding.py`, `ampere/core/simulate.py` (the context channel), `ampere/inference/_sbi.py`, `ampere/results/training.py`, `encoding.md`, `inference.md` §13, `docs/source/sbi.rst`, `tests/core/test_encoding*.py`, `tests/inference/test_sbi.py`; W5.8 owns `examples/m2_misspecification/`, `tests/m2/`, the sparsity-prior addition in `ampere/core/parameter.py` (the regularised horseshoe through `HierarchicalPrior`) and its lowering note, `ampere/core/kernels.py` only if a `Sum` amplitude hook is needed (append-only), `likelihoods.md` §6/§13, `docs/source/m2*.rst`; shared files append-only in delimited blocks. Then **wave 5** W5.12 ∥ W5.14; **wave 6** W5.15, W5.16, W5.17, W5.18 as slots free; **fillers** any time a slot is free: W5.21 (the GRID gate — now unblocked, Sonnet), W5.24 (heteroscedastic joint noise, Opus M, new), a second reduction of `tests/results/test_population.py`'s default cost (Sonnet), `SBIEngine.calibrate()` reseeding torch as `run()` does (Sonnet; W5.23's finding), a doctest over `docs/source/*.rst`'s `pycon` blocks (Sonnet; W5.7's finding); **W5.19 last**. (3) **Rulings — all six from W5.7 and W5.22 confirmed by Peter 2026-09-18 as recommended and recorded in both rows** (the warp-under-composite potential need and the hierarchical-prior marginalisation route noted for the future; W5.12's text carries the latter). Nothing is pending. (4) **Housekeeping worth Sonnet items when slots are free** (beside the fillers above): `replace_observations` also drops `shared=`/`shared_label=` (one line; W5.9); `ampere.results.sbc` ranks posterior variables only — a `derived=` hook (W5.9); a `summarise`-side helper reporting `B` rather than its bimodal angle (W5.9); `QuasisepGP.condition(at=...)` unusable on a multi-axis container (W5.7); `tests/` not an importable package; `_GriddedSolver` private but the extension point; `HilbertSpaceGP` forms two `(N, m)` blocks; WORK_ITEMS.md's W5.13 text cites §13.15 (landed as 16) and W5.22's text cites a `test_provenance*.py` that does not exist. (5) **Cleanup done 2026-09-18 on Peter's instruction**: every branch merged into master deleted — 79 item branches (`r1-…`, `w0.9` through `w5.23`) and 74 `worktree-agent-*` harness branches — leaving master and the three in-flight wave-4 branches; `git worktree prune` run; nothing is pushed (origin at `b8e585b`); his untracked paper script under `examples/examples_paper/` stays.

**Session record, 2026-09-16 22:20 → 2026-09-17 10:00 (the wave-3 session).** Recorded the wave-3 gate; took Peter's rulings on W5.5, W5.13, W5.4 and W5.6 (drafted W5.21–W5.23); dispatched W5.7 then W5.9 an hour later, then W5.23 and W5.22 into freed slots; reviewed all four on Fable's second pass by reading the mathematics and re-running each branch's rows (no fixes needed on any); Peter merged all four; one four-environment gate covered the first three (green); W5.24 drafted from Peter's heteroscedasticity ruling at W5.9's review. Agent usage: W5.7 498 k tokens / 377 tool uses / 99 min; W5.23 247 k / 158 / 47 min; W5.9 two segments (the resumed one 547 k / 14 / 6 min; the first lost to the kill); W5.22 ≈ 409 k / 298 over three segments. **Lessons**: (a) the session rate limit killed agents twice (03:15, 08:00); rule 8 held — every worktree was clean or had one edit, preserved as a WIP commit, and `SendMessage` to the dead agent's id resumes it from its transcript; (b) the harness's low-memory guard kills the orchestrator's background `sleep` waiters but not a `nohup setsid` gate — run gates and review test runs detached and read the log later; (c) a foreground review run with a ten-minute `timeout` is killed while queued on the lock behind an agent's run — launch review runs detached too; (d) a task-completion notification fires every time an agent yields to a background command — it is not the agent finishing; (e) `RUF022` forces sorted `__all__`, so parallel agents' "append at the end" collides anyway — trivial, keep both; (f) an agent told to merge the sibling's finished branch (or master) before reporting made every second merge clean.

**Previous head (2026-09-16 22:30, superseded above).** (1) **Dispatch wave 3: W5.7 ∥ W5.9** (both Opus, both L) if Peter has said go — otherwise ask him in one line. Base: master at the commit that carries this paragraph (the one recording the wave-3 gate; `git rev-parse master`). Each agent in a fresh worktree (`isolation: "worktree"`), installing `dev`, `torch` and `jax` itself in the background (≈ 2 GB and several minutes each; 177 GB free on disk). The prompts are the standing paragraph below plus the item's read-ranges, ownership and Accept list; the drafts from this session live only in its scratchpad, so a new session rewrites them from these facts. **Ownership**: W5.7 owns `ampere/core/kernels.py`, the kernel-twin sections of `backends/{torch,jax}/gp.py`, the solver section of `core/likelihood.py` (899–1757) and `Likelihood.conditional`, `tests/conformance/backends/_kernels.py`, `test_likelihoods.py` rows, `KernelFamily`, `likelihoods.md` §6/§8 and the quasiseparable-term paragraphs of §7; W5.9 owns the noise-model section of `likelihood.py` (1757–2268) plus a delimited new routing block in `Likelihood`, `backends/*/noise.py`, `DatasetCollection` in `core/dataset.py`, the `"joint"` decomposition in `inference`/`results`, `examples/astrometry/`, `test_astrometry.py`, `AstrometryPieces`, `likelihoods.md` §5/§15 and a subsection appended to §7, `inference.md` §4/§7, `results.md` §6; the astrometry twins are read-only; shared files (`core/__init__.py`, backends' `__init__.py`, capability flags in `protocol.py`) are append-only in delimited blocks. Merge order W5.7 then W5.9 (WORK_ITEMS.md's `likelihood.py` rule). Agents run only quick checks and targeted files through `flock /tmp/ampere-gate.lock`, never `test-all`; the orchestrator gates the merged wave once, detached, and records the four counts in both rows. Stagger the second dispatch by an hour so the pixi installs and targeted runs do not collide. **Merges are Peter's** (the auto-mode classifier refuses `git merge`): review, draft the merge message and the rows here, commit, ask Peter to run `git merge --no-ff <branch>` from the main checkout. (2) **Rulings — all given by Peter 2026-09-16 (evening), recorded in the rows and drafted as W5.21 (the GRID gate; depends on W5.7 + W5.9), W5.22 (`population_full` + `PriorSpec` in provenance, the next schema version) and W5.23 (explicit serving of a named cached SBI artefact) — three Sonnet fillers to dispatch when a slot is free, W5.22 and W5.23 any time, W5.21 after wave 3 merges.** The list as it stood: from W5.5 — the two-edit `likelihood.py` change lifting the `Layout.GRID` refusals (after which `examples/image/grid_gp.py` is deleted and `bakeoff.py` stops depending on it), and `--no-dense`; from W5.13 — a `population_full` marker on the 200-object row (it costs ≈ 10 min in dev and ≈ 20 in sbi inside every gate), and storing each parameter's `PriorSpec` in provenance; from W5.6 — *ruled 2026-09-16: the two prototypes stay exported from `ampere.core`* (the `VecchiaGP` slot's docstring pointer stays as is); from W5.4 — hashing `m`/`c`, `SpectralMixture` supported. Each is a one-line ruling; a "yes" on the W5.5 change becomes a Sonnet fix-up item. (3) **Housekeeping worth one Sonnet item when a slot is free**: `tests/examples/conftest.py` lacks the `sys.path` insertion `tests/m2`'s and `tests/interferometry`'s have (so `test-examples` fails at collection in a worktree); `tests/` is not an importable package; `_GriddedSolver` private but the extension point; `HilbertSpaceGP` forms two `(N, m)` blocks; a backend twin of EFGP would need a 2-D `condition` conformance row; WORK_ITEMS.md's W5.13 text cites `results.md` §13.15 where the item landed as 16. (4) The eight merged `w5.*` branches and the ~70 `worktree-agent-*` harness branches can be deleted at leisure (`git branch -d`; the harness ones via `git branch --list 'worktree-agent-*' | xargs -r git branch -d`); nothing is pushed (origin is still at `b8e585b`). (5) After wave 3: wave 4 W5.10 (+ W5.11) ∥ W5.8; wave 5 W5.12 ∥ W5.14; wave 6 W5.15, W5.16, W5.17, W5.18 as slots free; W5.19 last.

**W5.7 merged by Peter at `97f7662` (2026-09-17); status and decision-log rows pasted; the agent's worktree removed. The W5.9 agent was told to merge `w5.7-warped-kernel` into its branch before reporting.**

**W5.22 merged by Peter at `b16607f` (2026-09-17 ~10:00); rows pasted; the last worktree removed. Session closed by Peter after this merge.** **GATE RECORDED: the merged-wave gate (W5.7 + W5.23 + W5.9) finished 06:58 on 2026-09-17, all green — dev 2818/422, sbi 3891/95, torch 3757/229, jax 3577/238 — and the counts are in the three rows. Interruption 2 (2026-09-17 ~08:00): the session rate limit killed W5.22 again (reset 08:30) and the harness's low-memory guard killed the orchestrator's two gate waiters (the detached gate itself survived, as designed); W5.22's one uncommitted edit (`tests/results/test_results.py`) was preserved as a WIP commit on its branch and the agent resumed at 09:15.** **W5.9 merged by Peter at `8e10b55` (2026-09-17); rows pasted; worktree removed; his rulings recorded (heteroscedastic channels essential → W5.24 drafted; the study design accepted). The merged-wave gate (W5.7 + W5.23 + W5.9) launched detached on `8e10b55` at 04:13 on 2026-09-17 — legs dev → sbi → torch → jax, one summary line per leg in `/tmp/claude-1000/-home-pscicluna-GitHub-ampere/97357ead-1dac-41e5-aba5-b74825189e16/scratchpad/wave3-gate.log` (full output beside it as `.log.<env>`; `=== ALL DONE` closes it); record the four counts in the W5.7, W5.23 and W5.9 rows. Baseline, the previous gate on `8d785e1`: dev 2695/410, sbi 3744/94, torch 3614/224, jax 3434/233.** **Peter's plan (2026-09-17, ~04:30): after W5.22 finishes and merges, clean up to start a new session** — so the next steps are: W5.22's review and merge (it must merge master first; the markers-list conflict is expected), record the gate, remove W5.22's worktree, delete the merged `w5.*` and harness branches if Peter wants, and rewrite this head for the clean session (wave 4: W5.10 (+ W5.11) ∥ W5.8; W5.21 now unblocked; W5.24 new). **W5.23 merged by Peter at `05ff62f` (2026-09-17); rows pasted; worktree removed.** **W5.7 merged by Peter at `97f7662` (2026-09-17); status and decision-log rows pasted; the agent's worktree removed. The W5.9 agent was told to merge `w5.7-warped-kernel` into its branch before reporting.**

**W5.23 REVIEWED 2026-09-17, READY FOR PETER'S MERGE** (branch `w5.23-serve-artefact`, head `29f7173`, base `1f2d53c` — after W5.7's merge, so the merge is clean; 3 commits, 6 files, +418/−5; the agent: 247 k tokens / 158 tool uses / 47 min). Fable's review: the self-verifying `get_by_digest`, the served/mismatch attrs and the untouched default path read; the store rows and the four serving rows re-run in sbi on the branch (63 passed in 13 s); no fixes needed. **Peter runs, from the main checkout**: `git merge --no-ff w5.23-serve-artefact` with the message below. Then the orchestrator pastes the two rows below with the merge hash, removes the agent's worktree (`git worktree remove --force --force .claude/worktrees/agent-a841db80bebd96a38`), and tells the W5.9 agent to merge master again before reporting (W5.23 touches nothing W5.9 owns, so that merge is trivial).

*Merge message:*

```
Merge branch 'w5.23-serve-artefact'

W5.23: SBIEngine(cache=..., serve_artefact=<digest>) — an explicit route
to restore one named cached artefact regardless of the computed key,
with the served digest and the field-wise mismatch recorded in the run's
attrs and warned about. Sonnet-authored, Fable-reviewed; no fixes needed.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Y4vLKdyyMpn1e2MTqixBwM
```

*Status row (WORK_ITEMS.md, after W5.7's):*

| W5.23 | merged 2026-09-17 at `MERGE_HASH` (Sonnet-authored, Fable-reviewed — the self-verifying `get_by_digest` (the sidecar's recorded ingredients must hash back to the digest it is filed under, which is exactly `ArtefactKey.digest`'s definition), the served/mismatch attrs and the untouched default path read; the orchestrator's re-run of `tests/results/test_artefacts.py` and the serving rows in sbi: 63 passed in 13 s; no fixes needed; merged by Peter; the agent's runs: sbi `tests/inference/test_sbi.py` 163/2 in 294 s, `test_artefacts.py` 59/0, import sweep 105/2, lint/format/typecheck clean; the merged-wave gate to be recorded with W5.9; the agent: 247 k tokens / 158 tool uses / 47 min). Landed: `SBIEngine(cache=..., serve_artefact=<digest>)` — the ordinary key still computed and recorded, the named entry restored regardless through `ArtefactStore.get_by_digest` (a `(artefact, ingredients)` pair, or `None` with an `ArtefactCacheWarning` for a missing, unreadable, hand-edited or corrupt entry), a miss refused by name as `EngineError` rather than falling back to training, `serve_artefact` without `cache` refused at construction; attrs `sbi_artefact_served` and `sbi_artefact_mismatch` (field-wise, from the factored `ArtefactStore.diff_ingredients`, empty when they agree) beside `sbi_cache_key`, with an `ArtefactCacheWarning` naming the differing fields; `results.md` §9 documents all three (`sbi_cache_key` had been written since W3.5 but never listed); `docs/source/sbi.rst`'s cache section gains the explicit route; 9 store rows and 4 engine rows (the served-and-calibrated row differs on `budget`, the file's established one-ingredient case, since its fixtures have no HSGP problem). Carried: **`SBIEngine.calibrate()` does not reseed torch as `run()` does** (the agent found SBC/TARP thresholds in `test_sbi.py` sensitive to test order when a preceding test samples a posterior; its own row saves and restores `torch.get_rng_state()` around `calibrate()` as a local mitigation) — a follow-up for the next housekeeping item, W3.15's pattern applied to `calibrate()`. |

*Decision-log row (DEVELOPMENT_PLAN.md §2, after the W5.7 row):*

| An explicit route to serve one named cached SBI artefact, with the mismatch recorded (W5.23) | **`SBIEngine(cache=..., serve_artefact=<digest>)` restores the artefact filed under that digest regardless of whether it matches the run's own computed key, and the run's attrs say so** (merged MERGE_DATE, `MERGE_HASH`; Sonnet-authored, Fable-reviewed; from Peter's note of 2026-09-16 on W5.4's hashing). The content-addressed cache is unchanged and its rule stands: a posterior trained at one `basis_size` is a miss for another by construction, because two runs at different approximation parameters are not the same model. A user who accepts that risk names the artefact explicitly rather than getting a fuzzy match: `ArtefactStore.get_by_digest` looks an entry up by its filename digest alone and trusts it only by *self-verification* — the sidecar's recorded ingredients must hash back to the digest they are filed under, which is `ArtefactKey.digest`'s own definition — so a hand-edited or corrupt entry is refused with an `ArtefactCacheWarning` exactly as `get` refuses, and a missing digest is an `EngineError` rather than a fall-back to training. The run's attrs carry `sbi_cache_key` (the digest this run's settings would have used — written since W3.5, listed in `results.md` §9 only now), `sbi_artefact_served` and `sbi_artefact_mismatch` (field-wise, from the one factored `ArtefactStore.diff_ingredients`, empty when they agree), with a warning naming the differing fields; three engine-specific `extra=` attrs, none schema-counted, so no `PROVENANCE_SCHEMA_VERSION` bump. The default path is bitwise unchanged (W3.15's reproducibility row green, unmodified). |

**Session record, 2026-09-16 (evening, the preparation session).** Found: the wave-3 gate finished green at 13:45 (all four legs, launched 11:31 on `8d785e1`; the sbi, torch and jax legs ran on `27e9bd1`, which differs from `8d785e1` only by the previous handoff commit); no agents, no worktrees, master clean apart from Peter's untracked paper script. Done: the four counts recorded in the W5.5, W5.13 and W5.6 rows; this paragraph; the two wave-3 prompts drafted (standing paragraph + per-item read-ranges, ownership and Accept lists) and held for Peter's go. Machine: 9 GB free of 13, load idle.

**Previous head (2026-09-16, close of the crash-recovery session; superseded above).** (1) **Record the wave-3 gate.** It covers W5.5 + W5.13 + W5.6 and was launched detached (`nohup setsid`) on master `8d785e1` at 11:31 BST on 2026-09-16, legs dev → sbi → torch → jax through `flock /tmp/ampere-gate.lock`, one summary line per leg appended to `/tmp/claude-1000/-home-pscicluna-GitHub-ampere/6426b16c-0638-4a80-8f9e-302b49b5e7e6/scratchpad/wave3-gate.log` (full output in `wave3-gate.log.<env>` beside it; `=== ALL DONE` closes it). Expect dev ≈ 15–20 min, sbi ≈ 40–45, torch ≈ 25, jax ≈ 20 — the new 200-object population row is inside every leg. Check `pgrep -af wave3-gate.sh` and the log; if the machine rebooted or the log is missing, re-run each leg without a result line as `flock /tmp/ampere-gate.lock pixi run -e <env> test-all` on master, one at a time. Copy the four counts into the W5.5, W5.13 and W5.6 status rows (each says "the wave-3 merged-master gate to be recorded"). Baseline, the wave-2 gate on `db12df5`: dev 2594/407, sbi 3625/92, torch 3495/222, jax 3315/231. A failure comes back to a cheap fix-up agent with the log excerpt. (2) **Dispatch wave 3: W5.7 ∥ W5.9** (both Opus, both L; `WarpedKernel` and the shared-grid ICM) per WORK_ITEMS.md's ordering — two agents at a time, staggered, each in a fresh worktree with `pixi install -e dev` plus `torch` and `jax` (both items touch kernels/noise on every backend; a fresh install is ≈ 2 GB and several minutes per environment). Every prompt carries the standing dispatch paragraph below plus: prefix every command with a `cd` into the worktree, never touch the main checkout, `python -m pytest` not bare `pytest`, one test process at a time, every command under ten minutes, commit as units complete, report in the `work-item` skill's format with the decision-log text in the report. **Merges are Peter's**: the session's auto-mode classifier refuses `git merge`, so the orchestrator reviews, drafts the merge message and the rows into this section, commits, and asks Peter to run `git merge --no-ff <branch>` from the main checkout; the rows are pasted once his merge lands. (3) **Rulings pending for Peter** (all recorded in the rows): from W5.5 — the two-edit `likelihood.py` change lifting the `Layout.GRID` refusals (after which `examples/image/grid_gp.py` is deleted and `bakeoff.py` stops depending on it), and `--no-dense`; from W5.13 — a `population_full` marker on the 200-object row, and storing each parameter's `PriorSpec` in provenance; from W5.6 — whether the two prototypes stay exported from `ampere.core`'s public `__all__` (the agent exported them; reference-only, trivially reversible), and the `VecchiaGP` slot's docstring pointer; from W5.4 — hashing `m`/`c`, `SpectralMixture` supported. (4) **Housekeeping worth one Sonnet item when a slot is free**: `tests/examples/conftest.py` lacks the `sys.path` insertion `tests/m2`'s and `tests/interferometry`'s have, so `pixi run -e dev test-examples` fails at collection in a worktree (`python -m pytest` and `test-all` in the main checkout are unaffected); `tests/` is not an importable package (the `test_torch_device.py` complex rows); `_GriddedSolver` private but the extension point; `HilbertSpaceGP` forms two `(N, m)` blocks; a backend twin of EFGP would need a 2-D `condition` conformance row (the 1-D battery could not see the multilevel embedding bug W5.6 found and fixed); WORK_ITEMS.md's W5.13 text cites `results.md` §13.15 where the item landed as 16. (5) The eight `w5.*` branches are all merged and can be deleted at leisure (`git branch -d`); nothing is pushed (origin is still at `b8e585b`).

**Session record, 2026-09-16 (the crash-recovery session).** Found after the reboot: gate log and lock gone, both agents dead, branches and worktrees intact. Relaunched the wave-2 gate's three remaining legs (green), re-dispatched W5.5 and W5.13 as resume agents on their existing worktrees, reviewed and had Peter merge W5.5 (no fixes), found the interim-prior defect in W5.13 at review and had it fixed (the marginal prior, a required `interim_prior` argument), dispatched W5.6 into W5.5's freed worktree, reviewed and had Peter merge W5.13 and W5.6, wrote every row, removed all seven agent worktrees, launched the wave-3 gate. Agent usage: W5.5 resume 151 k tokens / 97 tool uses / 45 min; W5.13 resume 383 k / 117 / 53 min (including the fix); W5.6 322 k / 112 / 59 min. Lessons: the harness fires a completion notification each time an agent yields to a background command, which is not the agent stopping; `ruff format` must never be pointed at a `.md` file; keep the word "merge" out of Bash commands that are not merges (the classifier keyed on it once).

**Owed / carried from Phase 4** (beside the Phase 3 list further down): `NUTSEngine` tuning knobs (`target_accept`, `max_tree_depth`; W4.11); `examples/` excluded from ruff; `dataset.py`'s `part_name` docstring; a `Sum` containing a `Product` gets the generic O(N) refusal; W4.7's `BATCHABLE` unverified under `vmap`; four of the six plots refuse a point kind with several axes (Phase 5); `results/derived.py` casts complex to real with a warning in two places where a third refuses; `RiceFamily` declared-only with no schedule; `von_mises` not in `_TWINNED_FAMILIES` (native draws fall back to numpy); `encoding._sanitised`'s summary line over-promises; `QuasisepGP.conditional_loo` unimplemented on numpy; jax `bessel_jn` unusable across a realistic range and torch `bessel_j1` has no backward (documented on the uniform-disc models); two pre-existing docs-build warning pairs (`Binary`'s field list; duplicate `Product`/`Sum` descriptions); `interferometry.rst` attributes a non-verbatim quotation to the plan; the terra reviews owed on W4.1, W4.5 and W4.2 when the quota returns (~2026-09-30).

**The Phase 4 orchestration record** (for whoever reads only this): four agents dispatched in parallel on 2026-09-11 were killed twice by the session usage limit while polling gates with cold caches; the token-economy rules in `docs/orchestration.md` (agents never wait for gates; the orchestrator gates each merged wave once; bounded polling under the cache TTL) were adopted mid-phase and the second and later waves ran under them — every subsequent agent finished in 17–100 minutes. Two harness traps were re-hit and are in memory: a `cd` into a worktree persisting into a `git merge`, and a single Bash call exceeding ten minutes being pushed to the background.

**Where the project is.** Phases 0–3 complete: the spec frozen at `spec-v1.0` (2026-09-03), three backends in lockstep, M2 reached, and **the SBI layer closed 2026-09-10** — every Phase 3 item W3.0–W3.15 merged, W3.13 (the documentation pass) last at `0ff223e`. Each item's row in WORK_ITEMS.md's status table is the authoritative record of what landed, what was accepted at review and what is carried; the plan's §5 Phase 3 section carries the landed summary and §2 the decision-log rows. **Gates on master, all green, all four after the last code merge (W3.16, `fea7ef9`)**: dev 1996/268, sbi 2784/80, jax 2491/201, torch 2667/197. Lint/format/pyrefly clean in every environment. Lint/format/pyrefly clean in every environment. **Nothing is in flight, no agent worktrees exist, and nothing is pushed to origin** (origin/master is still at the W1.3 handoff, `b8e585b`). Master is clean apart from Peter's untracked paper script under `examples/examples_paper/` (leave it).

**Environments.** `dev`, `torch`, `jax`, and **`sbi` (= dev + torch feature + sbi extra since W3.2; run `pixi install -e sbi` after pulling)**. The gate is `pixi run -e <env> test-all` in each; an item's gate set is dev + sbi, plus torch and jax when it touches `ampere/core` or a backend. **Gates are serialised through `flock /tmp/ampere-gate.lock`** — every agent and the orchestrator run `flock /tmp/ampere-gate.lock pixi run -e <env> test-all`, so two five-suite runs never overlap on this 13 GB machine (the harness kills background tasks on low memory). Torch's takes 12–17 min, sbi's 14–20, jax's 8–13, dev's 5–8.

**What a clean session does next, in order.** (1) Read the "Everything that needs Peter's attention" block below first; any ruling Peter has given since becomes either a direct edit (the one-liners in part A: the `sample_with` default, the zero-rate guard, `cauchy`) or a small item — do those before anything new. (2) **The Phase 4 items are drafted** (WORK_ITEMS.md "Phase 4", `5488e6d`: W4.0–W4.9 and four decisions D1–D4 — the `ampere/modalities/` namespace, the `ClosurePhases` signature, whether W4.9 runs, the model split) **and await Peter's approval before any dispatch**; once approved, dispatch W4.0 ∥ W4.1 ∥ W4.5 ∥ W4.6 from the standing paragraph. The inputs they were drafted from: the inputs are the plan's §5 Phase 4 bullets (interferometric visibilities end to end — decided; the astropy interop adapter, §4.7; kernel algebra and the public quasiseparable-term registry, ruled into Phase 4 on 2026-09-09) and `docs/design/modalities/interferometry.md`, whose §9 (interface gaps), §10 (requirements on the joint fit) and §11 (open questions) are the item list in all but name; `docs/design/modalities/astrometric_timeseries.md` and `ifu_cube.md` are the follow-on templates. Draft in W2/W3 style — one branch per item, an Accept line each, a conformance row per new container or transformation, the gate set named — and size them S/M for Sonnet/Opus per `docs/orchestration.md`. (3) If Peter approves the memo's §5 adaptations or §9 horizons (part B), draft those as Phase 5 items in the same pass. (4) The owed small items below are Sonnet-sized fillers while gates run.

**Phase 3 in one paragraph, for whoever reads only this.** `ampere.inference.SBIEngine` over sbi 0.27 (NPE/NLE/NRE, and TMNRE through sbi's own ratio estimators — no swyft, ruled) fits any `FittingProblem` through `simulate_many`'s executor protocol (`forkserver` default, native batched prediction and sampling on torch and jax); the coordinate–value–mask encoding and the masked set/transformer embeddings; trained-artefact caching keyed on spec, model and data hashes plus the run's settings; SBC/TARP calibration usable by every engine; non-native parts refused by default and opt-in without gradients; core `sample()` for Poisson, Student-t and complex Gaussian with native twins; `PROVENANCE_SCHEMA_VERSION` 6 with `ampere_model_hash` and the append check; runs reproducible bitwise from the problem's seed; the blocking sbi CI leg; plot paging; the SBI tutorial. Carried into the deferred list: the embedding study with the ε study folded in; jax-native SBI on maturity.

**Standing dispatch paragraph** (put in every prompt): base commit and the ancestor/descendant recovery recipe from `docs/development.md` "Dispatching work"; the `flock` gate rule and the **five-minute rule** (a command longer than four minutes runs detached and is polled with `sleep 240; tail -3 <log>`, one poll per turn — the foreground `until`/`sleep` loop of earlier prompts is withdrawn, 2026-09-22); the **soft lifetime cap and hand-off** (250 k context / ~300 tool uses / 2.5 h; the `work-item` skill's Budget section has the protocol; the orchestrator dispatches the successor on the same branch within the hour); design documents and the two big modules read by range only; verification commands batched and trimmed; explicit file ownership when two agents run in parallel; do not edit the WORK_ITEMS.md status table or DEVELOPMENT_PLAN.md (decision-log text goes in the report); the commit trailer; the report format from the `work-item` skill. Models: Opus for judgement-within-spec, Sonnet for well-specified items (`docs/orchestration.md`).

**Decisions recorded this phase** (all in the plan's §2 or the status rows): `forkserver` default; native sampling the default; further core `sample` families on a use case with the pathway kept open (structurally enforced by W3.8) — met at W3.14; the mesh sharder's padding to be costed with the GPU item; the observation-context reservation (W3.3's encoding carries σ by default; `context=` accepted as `None` and recorded); kernel algebra into Phase 4; the embedding *study* and the ε study in the deferred list; W2.9's coverage study inside W3.6; the `marginals` group stored by default (W3.4); the Poisson/Student-t/complex-Gaussian sampling forms (W3.14); schema 6 and the model hash (W3.12); plot paging (W3.10); the seeding rule (W3.15); the columnar store for population work waits but nothing may exclude it (2026-09-10, horizon (b)).

**Rulings received 2026-09-15 (Peter), on the 2026-09-13 review block**: (1) W4.1's interferometric conventions **accepted**; (2) the four Fable-only decision-log rows **confirmed**; (3) W4.3's spelling: **(a) assumed provisionally**, and he asked for the consequences of the shadowing/duplication to be expanded — the analysis is in the plan's W4.3 row and W5.20's text (in one line: the reserved parameter names vary by backend because the check walks `type(self)`'s MRO, torch's `nn.Module` adds a third list, nothing reads a parameter as an attribute so the rule guards a convention, and the lookup order takes a class offering both pairs at the legacy name); (4) the token-economy rules: **continue as at present, re-evaluate later**; (5) W4.7's refusals and W4.10's path gating: he asked for more explanation — given in the session of 2026-09-15 (a tie is an opaque Python callable evaluated on floats, so on jax it cannot run on a tracer and on torch it would detach the gradient, and `|`/`&` have no elementwise reading as one channel's flux; results and inference are backend-neutral code that every engine on every backend writes through, so a change there skipping the torch or sbi leg is a change CI would not test) — awaiting his word; (6) W4.2's coverage pin **confirmed**. Phase 5: D2–D8 **as recommended** (D8: two Opus items, margins pinned); D1 provisional. **Closed later the same day**: D1 ruled as (a) and (b) together, W5.20 in; the W4.7/W4.10 explanations accepted ("the judgement calls make sense"). Nothing from the 2026-09-13 block remains open. The original block follows for the record.

**Awaiting Peter's review (2026-09-13; he will look "a little later")** — *ruled 2026-09-15, see above*: (1) W4.1's interferometric conventions — closure-phase sign and canonical ordering, baseline sign `r_j − r_i`, the DFT sign with x east and y north (`ClosurePhases`' docstring; `tests/conformance/oracles.py`'s header); (2) the four Phase 4 decision-log rows merged on Fable's second pass alone — W4.5 (Matérn-5/2 exactly quasiseparable, §15.3 withdrawn), W4.1's `VisibilitySet` amendment, the von Mises implementation, W4.2's `(n, k)` solver contract; (3) the token-economy proposal in `docs/orchestration.md` (three rules already in force); (4) W4.7's refusal of tied parameters and `|`/`&` on the native route, and W4.10's "results and inference count as core" for path gating; (5) W4.2's calibration row pinning coverage rather than the rank test where the GP over-covers. W4.11's far-infrared filter set was accepted the same day.

**Everything that needs Peter's attention, consolidated at the Phase 3 close (2026-09-10).** Rulings go in the status rows / decision log; nothing here blocks the close-out.

**Rulings received 2026-09-10 (Peter): parts A and B are all ruled and applied** — A1 and A6 landed as **W3.16** (merged `fea7ef9`; merged-master gates all green, recorded); A2–A4, A7, A8 confirmed in the decision-log rows; A5's principle in `likelihoods.md` §3; A9 as W4.10; A10 as W4.0 (1) and (6); B11–B14 and B17 in the memo's §10; B15's horizons (e)–(g) in the plan; B16 as W5.0. **Only part C remains open**, for Peter when he gets the chance. **Phase 3 is finished** (2026-09-10). The horizon notes are folded into the plan's §5 Phase 5 bullets and horizons (h)–(i) (`152d187`), so the whole horizon is visible in one place.

*A. Rulings on merged Phase 3 work (each is a one-line change or a "leave it")*
1. **W3.4 — `sample_with` default for TMNRE.** Current default `"rejection"` (i.i.d. draws). Measured 353.6 s against 43.8 s for `"mcmc"` on the example, and rejection's cost *rises* as truncation succeeds; `"mcmc"` is sbi's own default for ratio posteriors and what `method="nre"` uses. Recommendation: switch the default to `"mcmc"`, keep `"rejection"` selectable.
2. **W3.4 — the accuracy row.** The 0.5σ location tolerance is unreachable for a ratio estimator's joint on the toy problem; the suite asserts coverage plus the width band and holds the marginals to 1.0σ. Confirm, or state a tolerance.
3. **W3.4 — `ε = 1e-4`** gives a ±4σ box that stops shrinking after round 2. Recommendation: leave; the ε study is now in the deferred list (W3.13 entered it beside the embedding study).
4. **W3.4 — per-round pair estimators.** The 2-D marginal estimators train only on the final round; per-round pairs would cost budget for a box the 1-D marginals already set. Confirm the final-round-only choice.
5. **W3.14 — `cauchy`.** Refused only because the item listed three families; it is one branch away. Rule: a list (leave) or a principle (add it, and write the principle into `likelihoods.md` §3).
6. **W3.14 — the zero-rate asymmetry.** `PoissonFamily.sample` guards `rate < 0`, `log_prob` guards `rate <= 0`. Recommendation: align `sample` to `<= 0`.
7. **W3.12 — schema 6** (`ampere_model_hash` on every run and training set; a pre-schema-6 training set refused on append by name rather than guessed) and the `model_hash` name in place of the item's `model_identity_hash(problem)` (which already existed with other semantics). Confirm.
8. **W3.15** was drafted and dispatched on Fable's judgement: torch *and* numpy's legacy global generator seeded per step and restored. Confirm retroactively, or strike.
9. **W3.7 — the sbi CI leg** is likely the slowest (local 14–20 min against torch's 12–17). Accept, or apply a lever (typecheck split into its own job; tighter smoke budgets). Also: add `actionlint` to the dev toolchain?
10. **W3.13** — (a) `docs/source/overview.rst` is framed "as of M2" and still says five engines and "SBI is Phase 3": approve a small refresh item (W3.16). (b) No `examples/sbi` script does a bare in-process NPE fit; the tutorial covers the engine through the black-box case. Want a plain native example?

*B. The inference-extensions memo (`docs/design/inference_extensions_memo.md`)*
11. **§7.1 — the stored proposal density** as a rule for every approximate engine (VI today does not store it; SBI does). The hook with the widest consequences: importance correction, population reweighting at scale, retroactive density emulation. Yes/no.
12. **§7.2 — optimisation results**: a `DataTree` with an `optimum` group, or a typed object with `to_datatree()`.
13. **§7.3 — BO as an engine** or only as acquisition for emulation/multi-fidelity (recommendation: the latter; the *surrogate-posterior* family — VBMC, GPry — is filed separately at tier 2 as the route for simulators too expensive for SBI).
14. **§7.4 — dependency policy**: may `nautilus`/`ultranest` join the base install, or every new sampler behind an extra as `zeus` is?
15. **§9 — three new design horizons** proposed for the plan's list: (e) model comparison and averaging, (f) approximate-inference correction, (g) engines as a registry with uniform cost accounting. Approve and Fable adds them as one paragraph each; the per-horizon reservations in §9 (a fidelity column in training sets; the interim prior reconstructible from the archive; an emulator's identity including its training set; nested encodings) are sentences for the contracts they name, added by the first item that touches each.
16. **§5's six adaptations** — should they become drafted Phase 5 items now (the evidence attr, the weighted/approximate-draw rule, `ampere_approximation`, the `Optimum` result, batched `log_prob`, dependency tiers), or wait for the first engine that needs them? Recommendation: draft the first three as one small item, since they are results-contract text and the samplers of tier 1 want them.
17. **§8.6 / horizon (b) — the columnar store**: ruled "wait, but nothing may exclude it"; recorded as a constraint on horizon (b). Nothing further unless the ruling changes.

*C. Repository actions only Peter takes*
18. Push master (origin is still at the W1.3 handoff `b8e585b`) and watch the first live CI run: the branch-protection rule naming the old `phase1-suites` job needs `suites`, and **a required-check entry for `new-namespace suites (sbi)`** beside `(torch)`/`(jax)` (W3.7).
19. Delete the harness branches: `git branch --list 'worktree-agent-*' | xargs -r git branch -d`.
20. `pixi run gpu` on a machine with an accelerator, and cost the mesh sharder's padding there (W3.1 slice 2's ruling).
21. The retroactive `gpt-5.6-terra` reviews when the Codex quota returns (~2026-09-30): the Phase 2 queue (W2.3 first), then W3.1 slices 1–2, W3.2, W3.3, W3.8, W3.6's calibration statistics, W3.12's hash composition, W3.14's sampling forms.
22. `examples/examples_paper/flexible_likelihood_comparison.py` is untracked on master — his paper work; commit or ignore as he sees fit.

*D. Next phase*
23. **Phase 4 (one new modality end-to-end — interferometric visibilities, decided)** is the next to draft items for, with `docs/design/modalities/` as the input; Phase 5's inference items would follow from B. Fable drafts for approval before any dispatch.

**Owed / carried small items** (added 2026-09-13: `NUTSEngine` should expose `target_accept`/`max_tree_depth` — W4.11's modified-blackbody variant ran past 19 min at a 40/80 budget; `examples/` is excluded from ruff so example scripts escape the lint gate; `dataset.py:395`'s docstring cites `ampere.core.likelihood.Matern32`, now in `ampere.core.kernels`; a `Sum` containing a `Product` gets the generic O(N) refusal; W4.7's `BATCHABLE` unverified under `vmap`; **four of the six plots refuse a point kind with several axes** (`VisibilitySet`, `ClosurePhases`) — Phase 5's multi-axis diagnostics, W4.4's finding; `results/derived.py` casts complex to real with a warning in two places where a third refuses) (not scheduled; pick up when convenient, Sonnet-sized): a native sampler failure aborts a whole chunk where the numpy loop flags one draw (`_SimulateNatively.run`, W3.14's finding — wrap the sampler in the per-draw `try`); `Instrument._declarations()` fingerprints steps by `id()`, so a *bare* pickled `Instrument` still breaks (a value-based fingerprint in `transform.py`; `Dataset.__setstate__` works around it); torch `noise.py`'s `_check_one_device` partly redundant with the kernel-as-capability-part check; the "no uncertainty at all" branch of both backends' `_check_gp_uncertainty` is unreachable; upstream sbi 0.27 issues worth filing — `PermutationInvariantEmbedding` reads the first batch element's valid-row count for the whole batch, and `TransformerEmbedding` drops `attention_mask` unless causal; `z_score_x="none"` does not silence sbi's constant-column warning; the Phase 6 multi-observation composition tutorial (plan §5); the `tests/examples` smoke test of photometry + spectra composition.

**Operations notes for the orchestrator**: one five-suite gate at a time, always through the `flock`; run long gates detached (`nohup … &`) and wait with a background `until` loop rather than polling in the foreground; **never edit, format or check out source files in the main checkout while a gate runs there** (a half-written module crashed a subprocess test on 2026-09-09; markdown is safe); an agent that `cd`s to the main checkout can edit its files even though the isolation guard blocks git there — tell agents to stay in their worktree; after a lockfile change, `pixi install -e <env>` in the main checkout; the merged-master gate is the one that counts; parallel agents appending decision-log rows conflict at the table's last line — keep both rows, keep the table contiguous; agent worktrees under `.claude/worktrees/` may hold their own `.pixi` (2 GB) — remove a merged agent's worktree with `git worktree remove --force --force`; finished agents linger in the task list until dismissed with `TaskStop`; an agent that "stops to wait" for a gate is resumed with `SendMessage` and told to poll; a session rate limit kills agents mid-flight but their committed work survives — resume them from their branches.


---

Earlier session records (Phase 0–1 review outcomes, the 2026-09-01 to 2026-09-09 handoffs, including the incremental mid-Phase-3 record) are archived verbatim in `docs/handoff-archive.md`.
